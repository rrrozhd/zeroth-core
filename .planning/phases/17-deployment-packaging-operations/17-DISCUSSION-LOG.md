# Phase 17: Deployment Packaging & Operations - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-07
**Phase:** 17-deployment-packaging-operations
**Areas discussed:** Container topology, API versioning strategy, Health probe depth, TLS approach
**Mode:** Auto (all recommended defaults selected)

---

## Container Topology

| Option | Description | Selected |
|--------|-------------|----------|
| Single multi-stage Dockerfile + companion services | Zeroth gets one Dockerfile; Postgres, Redis, Regulus, sidecar use upstream images in compose | ✓ |
| Monorepo multi-image build | Build all custom images from one compose build | |
| Pre-built image registry | Push to registry, pull in compose | |

**User's choice:** [auto] Single multi-stage Dockerfile + companion services (recommended default)
**Notes:** GovernAI installed from GitHub commit pin. Regulus SDK handled via wheelhouse/COPY. Sandbox sidecar per Phase 14 SBX-02.

---

## API Versioning Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| APIRouter with /v1/ prefix + unversioned aliases | Mount all routes under /v1/, keep root aliases for backward compat | ✓ |
| URL path rewriting middleware | Middleware rewrites /v1/... to /... | |
| Header-based versioning | Accept-Version header selects API version | |

**User's choice:** [auto] APIRouter with /v1/ prefix + unversioned aliases (recommended default)
**Notes:** Health endpoints stay at root (not versioned). OpenAPI auto-generated by FastAPI for /v1/ routes.

---

## Health Probe Depth

| Option | Description | Selected |
|--------|-------------|----------|
| Readiness checks DB + Redis + optional Regulus; liveness is process-only | Structured JSON response with per-dependency status | ✓ |
| Single /health endpoint with query params | ?full=true for deep check, default is shallow | |
| Separate readiness per dependency | /health/db, /health/redis, /health/regulus | |

**User's choice:** [auto] Readiness checks DB + Redis + optional Regulus; liveness is process-only (recommended default)
**Notes:** Existing /health endpoint unchanged. Regulus optional — unavailability does not fail the probe.

---

## TLS Approach

| Option | Description | Selected |
|--------|-------------|----------|
| Nginx reverse proxy in compose + optional direct uvicorn SSL | Primary: Nginx terminates TLS. Fallback: ZEROTH_TLS_CERTFILE/KEYFILE for direct SSL | ✓ |
| Traefik with auto-cert | Traefik auto-discovers services, handles Let's Encrypt | |
| Uvicorn SSL only | No proxy, uvicorn handles TLS directly | |

**User's choice:** [auto] Nginx reverse proxy in compose + optional direct uvicorn SSL (recommended default)
**Notes:** Compose exposes 443 via Nginx, 8000 is internal. Direct SSL settings optional for simple deployments.

---

## Claude's Discretion

- Multi-stage Dockerfile layer structure
- Docker-compose service names and network topology
- Exact APIRouter wiring approach
- Health check timeout values
- Nginx configuration details
- OpenAPI metadata
- Development compose override file

## Deferred Ideas

None — discussion stayed within phase scope.
