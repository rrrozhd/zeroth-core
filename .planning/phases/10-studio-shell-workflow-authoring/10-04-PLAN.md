---
phase: 10-studio-shell-workflow-authoring
plan: 04
type: execute
wave: 4
depends_on: [10-02, 10-03]
files_modified:
  - apps/studio/src/app/AppShell.vue
  - apps/studio/src/features/header/StudioHeader.vue
  - apps/studio/src/features/modes/ModeSwitch.vue
  - apps/studio/src/features/workflows/WorkflowRail.vue
  - apps/studio/src/features/inspector/NodeInspector.vue
  - apps/studio/src/features/canvas/WorkflowCanvas.vue
autonomous: true
requirements: [STU-01, UX-01, UX-02]
must_haves:
  truths:
    - "Studio opens into a canvas-first shell rather than an operations dashboard."
    - "The left rail is workflows-first with folders and a secondary `Assets` entry."
    - "The shell exposes a header, an Editor or Executions or Tests mode switch, a dominant canvas, and a contextual inspector."
  artifacts:
    - path: "apps/studio/src/app/AppShell.vue"
      provides: "shell composition for rail, header, canvas, and inspector"
    - path: "apps/studio/src/features/workflows/WorkflowRail.vue"
      provides: "foldered workflow navigation and Assets utility entry"
    - path: "apps/studio/src/features/canvas/WorkflowCanvas.vue"
      provides: "dominant editor canvas surface"
  key_links:
    - from: "apps/studio/src/app/AppShell.vue"
      to: "apps/studio/src/features/workflows/WorkflowRail.vue"
      via: "shell layout imports workflow rail"
      pattern: "WorkflowRail"
    - from: "apps/studio/src/app/AppShell.vue"
      to: "apps/studio/src/features/canvas/WorkflowCanvas.vue"
      via: "shell layout centers the canvas surface"
      pattern: "WorkflowCanvas"
    - from: "apps/studio/src/features/workflows/WorkflowRail.vue"
      to: "apps/studio/src/lib/api/studio.ts"
      via: "rail fetches workflow summaries from the typed Studio client"
      pattern: "listWorkflows"
---

<objective>
Build the Phase 10 Studio shell layout on top of the scaffolded frontend workspace: workflow rail, header, mode switch, dominant canvas, and contextual inspector baseline.

Purpose: Finish the shell composition separately from package setup so this plan stays focused on user-visible layout and the locked quiet-shell posture.
Output: A buildable Studio shell that renders the approved canvas-first composition and workflow-centric navigation.
</objective>

<execution_context>
@/Users/dondoe/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dondoe/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_notes>
- Read the Phase 10 section in `PROGRESS.md` before implementation starts.
- Use the `progress-logger` skill after every meaningful unit of work, including test passes/failures and any changed deliverable.
- Capture artifact paths under `phases/phase-10-studio-shell-workflow-authoring/artifacts/` and update `PROGRESS.md` before reporting completion.
</execution_notes>

<context>
@.planning/ROADMAP.md
@PROGRESS.md
@.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md
@.planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md
@.planning/phases/10-studio-shell-workflow-authoring/10-UI-SPEC.md
@apps/studio/src/lib/api/studio.ts
@apps/studio/src/stores/studioShell.ts

<interfaces>
Shell structure required by the UI spec:
```text
Header: workflow title + save state + environment dropdown + publish/deploy controls
Main body: quiet left workflow rail | dominant center canvas | narrow right inspector
Mode switch labels: Editor, Executions, Tests
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shell layout with workflow rail, mode switch, canvas, and inspector</name>
  <files>apps/studio/src/app/AppShell.vue, apps/studio/src/features/header/StudioHeader.vue, apps/studio/src/features/modes/ModeSwitch.vue, apps/studio/src/features/workflows/WorkflowRail.vue, apps/studio/src/features/canvas/WorkflowCanvas.vue, apps/studio/src/features/inspector/NodeInspector.vue</files>
  <read_first>.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md, .planning/phases/10-studio-shell-workflow-authoring/10-UI-SPEC.md, docs/superpowers/specs/2026-03-29-zeroth-studio-design.md, apps/studio/src/app/AppShell.vue, apps/studio/src/features/header/StudioHeader.vue, apps/studio/src/features/modes/ModeSwitch.vue, apps/studio/src/features/workflows/WorkflowRail.vue, apps/studio/src/features/canvas/WorkflowCanvas.vue, apps/studio/src/features/inspector/NodeInspector.vue, apps/studio/src/stores/studioShell.ts, apps/studio/src/lib/api/studio.ts</read_first>
  <action>Implement the shell posture locked in UX-01 and UX-02. In `apps/studio/src/app/AppShell.vue`, compose a three-column layout with a quiet left rail width between `280px` and `320px`, a flexible center canvas region, and a narrow right inspector width between `320px` and `360px`. In `apps/studio/src/features/header/StudioHeader.vue`, render the active workflow title, save-state label, an environment dropdown trigger labeled with the current environment plus a `Manage environments` menu action, and two buttons labeled exactly `Run Draft` and `Publish`. In `apps/studio/src/features/modes/ModeSwitch.vue`, render a compact segmented control with labels exactly `Editor`, `Executions`, and `Tests`. In `apps/studio/src/features/workflows/WorkflowRail.vue`, fetch workflow summaries from the typed client, group them by `folder_path`, render a quiet list-oriented tree, and include a secondary utility row labeled `Assets` at the bottom instead of mixing assets into the tree. In `apps/studio/src/features/canvas/WorkflowCanvas.vue`, mount `@vue-flow/core` with placeholder nodes from the selected workflow graph and keep the center pane visually dominant. In `apps/studio/src/features/inspector/NodeInspector.vue`, render a narrow contextual panel with node title, contract summary placeholders, and compact recent activity placeholders; do not add deep runtime tables or asset-edit views in this plan. Keep accent usage limited to active mode, selected workflow row, keyboard focus ring, and selected node outline per the UI spec.</action>
  <verify>
    <automated>npm --prefix apps/studio run build</automated>
  </verify>
  <done>The Studio shell renders the approved canvas-first layout with workflow-focused navigation, contextual inspector, and mode switch while keeping runtime and governance detail ambient.</done>
</task>

</tasks>

<verification>
Run `npm --prefix apps/studio run build` and confirm the built app includes the shell composition, workflow-first rail, and canvas-dominant layout on top of the scaffolded workspace.
</verification>

<success_criteria>
The repository now contains a buildable `apps/studio` shell that opens into a workflow-first, minimal, contextual editor consistent with the approved UI contract.
</success_criteria>

<output>
After completion, create `.planning/phases/10-studio-shell-workflow-authoring/10-04-SUMMARY.md`
</output>
