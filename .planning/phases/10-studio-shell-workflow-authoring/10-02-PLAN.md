---
phase: 10-studio-shell-workflow-authoring
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/zeroth/studio/bootstrap.py
  - src/zeroth/studio/app.py
  - src/zeroth/studio/workflows_api.py
  - src/zeroth/studio/sessions_api.py
  - src/zeroth/studio/__init__.py
  - tests/studio/test_studio_app.py
autonomous: true
requirements: [STU-02]
must_haves:
  truths:
    - "A user can reach a dedicated Studio authoring API that is separate from the deployment-bound runtime wrapper."
    - "A Studio caller can list, create, and load workflow drafts only inside the tenant and workspace attached to their authenticated principal."
    - "A caller without the right role or workspace scope is rejected before Studio reads or writes occur, and lease conflicts still surface as HTTP 409."
  artifacts:
    - path: "src/zeroth/studio/bootstrap.py"
      provides: "StudioBootstrap container and bootstrap_studio entry point"
    - path: "src/zeroth/studio/app.py"
      provides: "FastAPI app factory with Studio authorization helpers"
    - path: "src/zeroth/studio/sessions_api.py"
      provides: "workspace-scoped lease acquire, renew, and release HTTP routes"
  key_links:
    - from: "src/zeroth/studio/app.py"
      to: "src/zeroth/studio/workflows_api.py"
      via: "route handlers resolve Studio principal scope before calling services"
      pattern: "require_studio_"
    - from: "src/zeroth/studio/workflows_api.py"
      to: "src/zeroth/studio/workflows/service.py"
      via: "workflow handlers pass tenant_id and workspace_id from the authenticated principal"
      pattern: "workspace_id"
    - from: "src/zeroth/studio/sessions_api.py"
      to: "src/zeroth/studio/leases/service.py"
      via: "lease handlers enforce scoped ownership and conflict behavior"
      pattern: "lease_service\\."
---

<objective>
Expose the Studio authoring app and HTTP surface on the backend: dedicated bootstrap, workflow list/create/detail routes, and lease lifecycle routes with explicit tenant/workspace scope enforcement.

Purpose: Keep the backend app/bootstrap/API wiring isolated from both persistence foundations and frontend shell work while making authorization rules concrete enough for frontend plans to depend on them safely.
Output: A workspace-scoped FastAPI authoring app with tested workflow and lease endpoints, plus explicit Studio role and scope enforcement.
</objective>

<execution_context>
@/Users/dondoe/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dondoe/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_notes>
- Read the Phase 10 section in `PROGRESS.md` before implementation starts.
- Use the `progress-logger` skill after every meaningful unit of work, including test passes/failures and any changed deliverable.
- Capture artifact paths under `phases/phase-10-studio-shell-workflow-authoring/artifacts/` and update `PROGRESS.md` before reporting completion.
</execution_notes>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@PROGRESS.md
@.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md
@.planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md
@src/zeroth/service/app.py
@src/zeroth/service/bootstrap.py
@src/zeroth/service/auth.py
@tests/service/helpers.py

<interfaces>
Use existing runtime auth roles and scoped test helpers:
```python
class ServiceRole(StrEnum):
    ADMIN = "admin"
    OPERATOR = "operator"
    REVIEWER = "reviewer"
```

Studio route behavior required in this plan:
```python
# Allowed Studio roles: ADMIN, OPERATOR
# Required principal fields: tenant_id, workspace_id
# Reject missing auth -> 401
# Reject REVIEWER or missing workspace scope -> 403
# Reject cross-workspace resource access -> 404
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose Studio bootstrap and workflow and lease HTTP APIs with scope enforcement</name>
  <files>src/zeroth/studio/bootstrap.py, src/zeroth/studio/app.py, src/zeroth/studio/workflows_api.py, src/zeroth/studio/sessions_api.py, src/zeroth/studio/__init__.py, tests/studio/test_studio_app.py</files>
  <read_first>.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md, .planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md, src/zeroth/service/app.py, src/zeroth/service/bootstrap.py, src/zeroth/service/auth.py, tests/service/helpers.py, src/zeroth/studio/bootstrap.py, src/zeroth/studio/app.py, src/zeroth/studio/workflows_api.py, src/zeroth/studio/sessions_api.py, tests/studio/test_studio_app.py</read_first>
  <behavior>
    - Test 1: `GET /studio/workflows` and `POST /studio/workflows` succeed for an authenticated `operator` or `admin` principal with a non-null `workspace_id`, and responses include `workflow_id`, `workspace_id`, `name`, `folder_path`, `draft_graph_version`, and `validation_status`
    - Test 2: the same routes return `401` without credentials, `403` for a `reviewer` principal or any principal with `workspace_id=None`, and `404` when a caller from a different workspace requests `GET /studio/workflows/{workflow_id}`
    - Test 3: `POST /studio/workflows/{workflow_id}/leases`, `POST /studio/workflows/{workflow_id}/leases/ping`, and `DELETE /studio/workflows/{workflow_id}/leases/{lease_token}` enforce workspace ownership and return `409` for a conflicting active lease in the same workspace
  </behavior>
  <action>Follow the existing FastAPI bootstrap pattern but create a dedicated authoring app per the locked delivery decision. In `src/zeroth/studio/bootstrap.py`, define `StudioBootstrap` with fields `workflow_service`, `lease_service`, `graph_repository`, `contract_registry`, `auth_config`, and `authenticator`, plus `bootstrap_studio(database: SQLiteDatabase, auth_config: ServiceAuthConfig | None = None, bearer_token_verifier: JWTBearerTokenVerifier | None = None) -> StudioBootstrap`. In `src/zeroth/studio/app.py`, create `create_studio_app(bootstrap: StudioBootstrapLike) -> FastAPI`, add the same auth middleware pattern used by `src/zeroth/service/app.py`, and implement a shared helper such as `require_studio_principal(request, allowed_roles={ServiceRole.OPERATOR, ServiceRole.ADMIN})` that fails with `403` when `workspace_id` is absent because Studio is workspace-scoped. In `src/zeroth/studio/workflows_api.py`, register `GET /studio/workflows`, `POST /studio/workflows`, and `GET /studio/workflows/{workflow_id}` using typed request/response models with `ConfigDict(extra="forbid")`; route handlers must pass `principal.tenant_id` and `principal.workspace_id` into the workflow service for every read and write. In `src/zeroth/studio/sessions_api.py`, register `POST /studio/workflows/{workflow_id}/leases`, `POST /studio/workflows/{workflow_id}/leases/ping`, and `DELETE /studio/workflows/{workflow_id}/leases/{lease_token}` with the same scoped principal enforcement. Use the existing service auth system from `zeroth.service.auth`; Studio authoring permissions map to `ServiceRole.OPERATOR` and `ServiceRole.ADMIN` only. Add `tests/studio/test_studio_app.py` using `fastapi.testclient.TestClient`, `tests/service/helpers.scoped_auth_config()`, and mixed-tenant/workspace fixtures to prove app bootstrap, correct response shapes, rejection of anonymous, reviewer, and unscoped principals, rejection of cross-workspace reads, and surface of lease conflicts over HTTP.</action>
  <verify>
    <automated>uv run pytest tests/studio/test_studio_app.py -q</automated>
  </verify>
  <done>The repo has a new Studio FastAPI app with workflow list/create/detail routes, lease lifecycle routes, and tests proving Studio access is limited to authenticated operator or admin callers inside the owning tenant/workspace.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/studio/test_studio_app.py -q` and confirm the new Studio package exposes workflow and lease APIs through a dedicated authoring app while rejecting anonymous, wrong-role, and wrong-workspace access.
</verification>

<success_criteria>
The repository now contains a tested, dedicated Studio authoring app that exposes workspace-scoped workflow and lease routes for frontend consumers while preserving the Phase 10 draft-vs-runtime boundary and enforcing Studio permissions explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/10-studio-shell-workflow-authoring/10-02-SUMMARY.md`
</output>
