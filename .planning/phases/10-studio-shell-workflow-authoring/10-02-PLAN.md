---
phase: 10-studio-shell-workflow-authoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/studio/package.json
  - apps/studio/tsconfig.json
  - apps/studio/vite.config.ts
  - apps/studio/index.html
  - apps/studio/src/main.ts
  - apps/studio/src/router/index.ts
  - apps/studio/src/app/AppShell.vue
  - apps/studio/src/styles/tokens.css
  - apps/studio/src/stores/studioShell.ts
  - apps/studio/src/lib/api/studio.ts
  - apps/studio/src/features/header/StudioHeader.vue
  - apps/studio/src/features/modes/ModeSwitch.vue
  - apps/studio/src/features/workflows/WorkflowRail.vue
  - apps/studio/src/features/canvas/WorkflowCanvas.vue
  - apps/studio/src/features/inspector/NodeInspector.vue
autonomous: true
requirements: [STU-01, UX-01, UX-02]
must_haves:
  truths:
    - "Studio opens into a canvas-first shell rather than an operations dashboard."
    - "The left rail is workflows-first with folders and a secondary Assets entry."
    - "The shell exposes Header, Editor/Executions/Tests mode switch, dominant canvas, and contextual inspector."
  artifacts:
    - path: "apps/studio/package.json"
      provides: "Vue 3 + Vite Studio workspace and scripts"
    - path: "apps/studio/src/app/AppShell.vue"
      provides: "shell composition for rail, header, canvas, and inspector"
    - path: "apps/studio/src/features/workflows/WorkflowRail.vue"
      provides: "foldered workflow navigation and Assets utility entry"
  key_links:
    - from: "apps/studio/src/main.ts"
      to: "apps/studio/src/router/index.ts"
      via: "Vue app bootstrap"
      pattern: "app\\.use\\(router\\)"
    - from: "apps/studio/src/app/AppShell.vue"
      to: "apps/studio/src/features/workflows/WorkflowRail.vue"
      via: "shell layout imports workflow rail"
      pattern: "WorkflowRail"
    - from: "apps/studio/src/lib/api/studio.ts"
      to: "/studio/workflows"
      via: "typed fetch client"
      pattern: "/studio/workflows"
---

<objective>
Create the first frontend Studio shell as a standalone Vue 3 + Vite app: canvas-first layout, workflow rail, header, mode switch, and inspector baseline wired to typed Studio API contracts.

Purpose: Satisfy STU-01, UX-01, and UX-02 with the approved quiet-shell posture before adding deeper validation or autosave behavior.
Output: An `apps/studio` workspace that builds successfully and renders the approved shell structure with real route and data contracts.
</objective>

<execution_context>
@/Users/dondoe/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dondoe/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md
@.planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md
@.planning/phases/10-studio-shell-workflow-authoring/10-UI-SPEC.md
@docs/superpowers/specs/2026-03-29-zeroth-studio-design.md

<interfaces>
Frontend must target these Studio API contracts from Plan 10-01:
```ts
type WorkflowSummary = {
  workflow_id: string;
  name: string;
  folder_path: string;
  draft_graph_version: number;
  validation_status: "unknown" | "valid" | "invalid";
  lease_owner_subject: string | null;
};

type WorkflowDetail = {
  workflow_id: string;
  name: string;
  folder_path: string;
  revision_token: string;
  graph: {
    graph_id: string;
    version: number;
    entry_step: string;
    nodes: Array<Record<string, unknown>>;
    edges: Array<Record<string, unknown>>;
  };
};
```

Shell structure required by the UI spec:
```text
Header: workflow title + save state + environment dropdown + publish/deploy controls
Main body: quiet left workflow rail | dominant center canvas | narrow right inspector
Mode switch labels: Editor, Executions, Tests
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the standalone Vue 3 + Vite Studio workspace</name>
  <files>apps/studio/package.json, apps/studio/tsconfig.json, apps/studio/vite.config.ts, apps/studio/index.html, apps/studio/src/main.ts, apps/studio/src/router/index.ts, apps/studio/src/styles/tokens.css, apps/studio/src/stores/studioShell.ts, apps/studio/src/lib/api/studio.ts</files>
  <read_first>.planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md, .planning/phases/10-studio-shell-workflow-authoring/10-UI-SPEC.md, docs/superpowers/specs/2026-03-29-zeroth-studio-design.md, pyproject.toml, apps/studio/package.json, apps/studio/tsconfig.json, apps/studio/vite.config.ts, apps/studio/src/main.ts, apps/studio/src/router/index.ts, apps/studio/src/styles/tokens.css, apps/studio/src/stores/studioShell.ts, apps/studio/src/lib/api/studio.ts</read_first>
  <action>Create a standalone frontend package at `apps/studio` using the researched stack exactly: runtime dependencies `vue@3.5.31`, `vue-router@5.0.4`, `pinia@3.0.4`, `@vue-flow/core@1.48.2`, `@tanstack/vue-query@5.95.2`, `zod@4.3.6`, and `lucide-vue-next@1.0.0`; dev dependencies `vite@8.0.3`, `@vitejs/plugin-vue@6.0.5`, `typescript@6.0.2`, and `vue-tsc`. In `apps/studio/package.json`, add scripts `dev`, `build`, and `typecheck` where `build` runs `vue-tsc --noEmit && vite build`. In `apps/studio/src/main.ts`, bootstrap Vue with Pinia, Vue Router, and TanStack Query. In `apps/studio/src/router/index.ts`, define three top-level routes named exactly `editor`, `executions`, and `tests`, with `editor` as default. In `apps/studio/src/lib/api/studio.ts`, implement a typed fetch client targeting `/studio/workflows` and `/studio/workflows/:workflowId` with the `WorkflowSummary` and `WorkflowDetail` shapes from the interface block. In `apps/studio/src/styles/tokens.css`, declare CSS custom properties matching the approved UI spec exactly: `--space-xs: 4px`, `--space-sm: 8px`, `--space-md: 16px`, `--space-lg: 24px`, `--space-xl: 32px`, `--space-2xl: 48px`, `--space-3xl: 64px`, `--color-bg: #F4F7F8`, `--color-surface: #E6ECEF`, `--color-accent: #0F766E`, `--color-destructive: #C2410C`, font family `Instrument Sans`, and typography tokens for 12px labels, 14px body, 20px heading, 28px display.</action>
  <verify>
    <automated>npm --prefix apps/studio run build</automated>
  </verify>
  <acceptance_criteria>`rg -n '"build": "vue-tsc --noEmit && vite build"' apps/studio/package.json` matches once; `rg -n "name: 'editor'|name: \"editor\"" apps/studio/src/router/index.ts` matches once; `rg -n -- "--color-accent: #0F766E" apps/studio/src/styles/tokens.css` matches once; `rg -n "/studio/workflows" apps/studio/src/lib/api/studio.ts` matches at least twice; `npm --prefix apps/studio run build` exits 0.</acceptance_criteria>
  <done>`apps/studio` builds as a standalone Vue app, includes the approved design tokens, and has typed clients for workflow list/detail routes.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Phase 10 shell layout with workflow rail, mode switch, canvas, and inspector</name>
  <files>apps/studio/src/app/AppShell.vue, apps/studio/src/features/header/StudioHeader.vue, apps/studio/src/features/modes/ModeSwitch.vue, apps/studio/src/features/workflows/WorkflowRail.vue, apps/studio/src/features/canvas/WorkflowCanvas.vue, apps/studio/src/features/inspector/NodeInspector.vue, apps/studio/src/stores/studioShell.ts, apps/studio/src/lib/api/studio.ts</files>
  <read_first>.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md, .planning/phases/10-studio-shell-workflow-authoring/10-UI-SPEC.md, docs/superpowers/specs/2026-03-29-zeroth-studio-design.md, apps/studio/src/app/AppShell.vue, apps/studio/src/features/header/StudioHeader.vue, apps/studio/src/features/modes/ModeSwitch.vue, apps/studio/src/features/workflows/WorkflowRail.vue, apps/studio/src/features/canvas/WorkflowCanvas.vue, apps/studio/src/features/inspector/NodeInspector.vue, apps/studio/src/stores/studioShell.ts, apps/studio/src/lib/api/studio.ts</read_first>
  <action>Implement the shell posture locked in UX-01 and UX-02. In `apps/studio/src/app/AppShell.vue`, compose a three-column layout with a quiet left rail width between `280px` and `320px`, a flexible center canvas region, and a narrow right inspector width between `320px` and `360px`. In `apps/studio/src/features/header/StudioHeader.vue`, render the active workflow title, save-state label, an environment dropdown trigger labeled with the current environment plus a `Manage environments` menu action, and two buttons labeled exactly `Run Draft` and `Publish`. In `apps/studio/src/features/modes/ModeSwitch.vue`, render a compact segmented control with labels exactly `Editor`, `Executions`, and `Tests`. In `apps/studio/src/features/workflows/WorkflowRail.vue`, fetch workflow summaries from `/studio/workflows`, group them by `folder_path`, render a quiet list-oriented tree, and include a secondary utility row labeled `Assets` at the bottom instead of mixing assets into the tree. In `apps/studio/src/features/canvas/WorkflowCanvas.vue`, mount `@vue-flow/core` with placeholder nodes from the selected workflow graph and keep the center pane visually dominant. In `apps/studio/src/features/inspector/NodeInspector.vue`, render a narrow contextual panel with node title, contract summary placeholders, and compact recent activity placeholders; do not add deep runtime tables or asset-edit views in this plan. Keep accent usage limited to active mode, selected workflow row, keyboard focus ring, and selected node outline per the UI spec.</action>
  <verify>
    <automated>npm --prefix apps/studio run build</automated>
  </verify>
  <acceptance_criteria>`rg -n "Run Draft" apps/studio/src/features/header/StudioHeader.vue` matches once; `rg -n "Manage environments" apps/studio/src/features/header/StudioHeader.vue` matches once; `rg -n "Editor|Executions|Tests" apps/studio/src/features/modes/ModeSwitch.vue` matches all three labels; `rg -n "Assets" apps/studio/src/features/workflows/WorkflowRail.vue` matches once; `rg -n "@vue-flow/core|VueFlow" apps/studio/src/features/canvas/WorkflowCanvas.vue` matches once; `npm --prefix apps/studio run build` exits 0.</acceptance_criteria>
  <done>The Studio shell renders the approved canvas-first layout with workflow-focused navigation, contextual inspector, and mode switch while keeping runtime/governance detail ambient.</done>
</task>

</tasks>

<verification>
Run `npm --prefix apps/studio run build` and confirm the built app includes the shell composition, tokens, and typed workflow client for the Studio backend.
</verification>

<success_criteria>
The repository now contains a buildable `apps/studio` Vue app that opens into a workflow-first shell with quiet navigation, dominant canvas, header controls, and a contextual inspector consistent with the approved UI contract.
</success_criteria>

<output>
After completion, create `.planning/phases/10-studio-shell-workflow-authoring/10-02-SUMMARY.md`
</output>
