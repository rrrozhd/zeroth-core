---
phase: 10-studio-shell-workflow-authoring
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - apps/studio/package.json
  - apps/studio/tsconfig.json
  - apps/studio/vite.config.ts
  - apps/studio/src/main.ts
  - apps/studio/src/router/index.ts
  - apps/studio/src/styles/tokens.css
  - apps/studio/src/stores/studioShell.ts
  - apps/studio/src/lib/api/studio.ts
autonomous: true
requirements: [STU-01, UX-01, UX-02]
must_haves:
  truths:
    - "Studio boots as a standalone canvas-first web app rather than as an operations add-on."
    - "The frontend has typed contracts for workspace-scoped workflow list and detail loading before shell components consume them."
    - "The app workspace can run build and test commands before shell composition begins."
  artifacts:
    - path: "apps/studio/package.json"
      provides: "Vue 3 + Vite Studio workspace, scripts, and test tooling"
    - path: "apps/studio/src/router/index.ts"
      provides: "Editor, Executions, and Tests shell route skeleton"
    - path: "apps/studio/src/lib/api/studio.ts"
      provides: "typed Studio workflow and lease client contracts for later plans"
  key_links:
    - from: "apps/studio/src/main.ts"
      to: "apps/studio/src/router/index.ts"
      via: "Vue app bootstrap"
      pattern: "app\\.use\\(router\\)"
    - from: "apps/studio/src/main.ts"
      to: "apps/studio/src/stores/studioShell.ts"
      via: "Pinia store registration"
      pattern: "createPinia"
    - from: "apps/studio/src/lib/api/studio.ts"
      to: "/studio/workflows"
      via: "typed fetch client"
      pattern: "/studio/workflows"
---

<objective>
Scaffold the standalone Studio frontend workspace and typed API contracts without yet building the full shell composition.

Purpose: Reduce the scope of the original frontend plan by separating project setup and data contracts from visual shell assembly, so downstream plans can implement against stable interfaces.
Output: A buildable and testable `apps/studio` Vue workspace with route skeleton, design tokens, state store, and typed Studio API client contracts.
</objective>

<execution_context>
@/Users/dondoe/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dondoe/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_notes>
- Read the Phase 10 section in `PROGRESS.md` before implementation starts.
- Use the `progress-logger` skill after every meaningful unit of work, including test passes/failures and any changed deliverable.
- Capture artifact paths under `phases/phase-10-studio-shell-workflow-authoring/artifacts/` and update `PROGRESS.md` before reporting completion.
</execution_notes>

<context>
@.planning/ROADMAP.md
@PROGRESS.md
@.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md
@.planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md
@.planning/phases/10-studio-shell-workflow-authoring/10-UI-SPEC.md
@src/zeroth/studio/workflows_api.py
@src/zeroth/studio/sessions_api.py

<interfaces>
Frontend must target these backend contracts from Plans 10-01 and 10-02:
```ts
type WorkflowSummary = {
  workflow_id: string;
  workspace_id: string;
  name: string;
  folder_path: string;
  draft_graph_version: number;
  validation_status: "unknown" | "valid" | "invalid";
  lease_owner_subject: string | null;
};

type WorkflowDetail = {
  workflow_id: string;
  workspace_id: string;
  name: string;
  folder_path: string;
  revision_token: string;
  graph: {
    graph_id: string;
    version: number;
    entry_step: string;
    nodes: Array<Record<string, unknown>>;
    edges: Array<Record<string, unknown>>;
  };
};

type LeasePayload = {
  workflow_id: string;
  workspace_id: string;
  lease_token: string;
  expires_at: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the standalone Vue 3 + Vite Studio workspace and typed API contracts</name>
  <files>apps/studio/package.json, apps/studio/tsconfig.json, apps/studio/vite.config.ts, apps/studio/src/main.ts, apps/studio/src/router/index.ts, apps/studio/src/styles/tokens.css, apps/studio/src/stores/studioShell.ts, apps/studio/src/lib/api/studio.ts</files>
  <read_first>.planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md, .planning/phases/10-studio-shell-workflow-authoring/10-UI-SPEC.md, docs/superpowers/specs/2026-03-29-zeroth-studio-design.md, pyproject.toml, apps/studio/package.json, apps/studio/tsconfig.json, apps/studio/vite.config.ts, apps/studio/src/main.ts, apps/studio/src/router/index.ts, apps/studio/src/styles/tokens.css, apps/studio/src/stores/studioShell.ts, apps/studio/src/lib/api/studio.ts</read_first>
  <action>Create a standalone frontend package at `apps/studio` using the researched stack exactly: runtime dependencies `vue@3.5.31`, `vue-router@5.0.4`, `pinia@3.0.4`, `@vue-flow/core@1.48.2`, `@tanstack/vue-query@5.95.2`, `zod@4.3.6`, and `lucide-vue-next@1.0.0`; dev dependencies `vite@8.0.3`, `@vitejs/plugin-vue@6.0.5`, `typescript@6.0.2`, `vue-tsc`, `vitest`, and `jsdom`. In `apps/studio/package.json`, add scripts `dev`, `build`, `typecheck`, and `test` where `build` runs `vue-tsc --noEmit && vite build` and `test` runs `vitest --run`. In `apps/studio/src/main.ts`, bootstrap Vue with Pinia, Vue Router, and TanStack Query. In `apps/studio/src/router/index.ts`, define three top-level routes named exactly `editor`, `executions`, and `tests`, with `editor` as default. In `apps/studio/src/lib/api/studio.ts`, implement typed client methods for `listWorkflows`, `getWorkflow`, `acquireWorkflowLease`, `renewWorkflowLease`, and `releaseWorkflowLease`, targeting the workspace-scoped routes from Plan 10-02 and carrying the `WorkflowSummary`, `WorkflowDetail`, and `LeasePayload` types from the interface block. In `apps/studio/src/stores/studioShell.ts`, define the minimal shell store contract for selected workflow ID, selected node ID, current route mode, save status, and current `leaseToken`, but do not implement autosave orchestration yet. In `apps/studio/src/styles/tokens.css`, declare the approved CSS custom properties exactly: `--space-xs: 4px`, `--space-sm: 8px`, `--space-md: 16px`, `--space-lg: 24px`, `--space-xl: 32px`, `--space-2xl: 48px`, `--space-3xl: 64px`, `--color-bg: #F4F7F8`, `--color-surface: #E6ECEF`, `--color-accent: #0F766E`, `--color-destructive: #C2410C`, font family `Instrument Sans`, and typography tokens for 12px labels, 14px body, 20px heading, 28px display.</action>
  <verify>
    <automated>npm --prefix apps/studio run build</automated>
  </verify>
  <done>`apps/studio` builds as a standalone Vue app, exposes stable workflow and lease client contracts, and includes the approved shell tokens before visual composition starts.</done>
</task>

</tasks>

<verification>
Run `npm --prefix apps/studio run build` and confirm the workspace boots with the route skeleton, design tokens, and typed Studio API contract layer.
</verification>

<success_criteria>
The repository now contains a buildable `apps/studio` Vue workspace with stable API contracts and editor-shell scaffolding that later plans can compose without revisiting package or router setup.
</success_criteria>

<output>
After completion, create `.planning/phases/10-studio-shell-workflow-authoring/10-03-SUMMARY.md`
</output>
