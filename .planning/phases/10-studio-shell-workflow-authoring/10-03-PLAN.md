---
phase: 10-studio-shell-workflow-authoring
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - src/zeroth/studio/workflows/service.py
  - src/zeroth/studio/workflows_api.py
  - src/zeroth/studio/validation_api.py
  - src/zeroth/studio/app.py
  - tests/studio/test_validation_api.py
  - apps/studio/src/lib/api/studio.ts
  - apps/studio/src/lib/api/validation.ts
  - apps/studio/src/features/validation/ValidationSummary.vue
  - apps/studio/src/features/inspector/ContractPanel.vue
  - apps/studio/src/features/inspector/NodeInspector.vue
  - apps/studio/src/features/canvas/WorkflowCanvas.vue
  - apps/studio/src/stores/studioShell.ts
autonomous: true
requirements: [STU-02, AST-04, UX-01]
must_haves:
  truths:
    - "Structural graph edits autosave against the current draft with lease and revision-token protection."
    - "Contract and validation feedback appears in node-local editor flows rather than a detached contract library."
    - "Editor mode stays minimal while still surfacing immediate validation failures and compact recent node activity."
  artifacts:
    - path: "src/zeroth/studio/validation_api.py"
      provides: "validation and contract lookup endpoints for authoring flows"
    - path: "apps/studio/src/features/validation/ValidationSummary.vue"
      provides: "local validation rendering in the shell"
    - path: "apps/studio/src/features/inspector/ContractPanel.vue"
      provides: "node-local contract configuration UI"
  key_links:
    - from: "apps/studio/src/features/canvas/WorkflowCanvas.vue"
      to: "src/zeroth/studio/workflows_api.py"
      via: "autosave mutation to draft route"
      pattern: "updateWorkflowDraft|/studio/workflows/.*/draft"
    - from: "apps/studio/src/features/inspector/NodeInspector.vue"
      to: "src/zeroth/studio/validation_api.py"
      via: "node-local validation and contract lookups"
      pattern: "validateWorkflow|lookupContract"
    - from: "src/zeroth/studio/validation_api.py"
      to: "src/zeroth/graph/validation.py"
      via: "GraphValidator.validate"
      pattern: "GraphValidator"
---

<objective>
Add the authoring behaviors that make the shell usable: lease-aware autosave for structural edits, backend-authoritative validation, and node-local contract configuration UX inside the inspector.

Purpose: Finish the Phase 10 outcome by covering AST-04 and the save/validation behaviors described in the design spec without bloating Editor mode.
Output: Validation and contract APIs, protected draft-save routes, and frontend inspector/canvas behaviors that surface immediate issues locally.
</objective>

<execution_context>
@/Users/dondoe/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dondoe/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md
@.planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md
@.planning/phases/10-studio-shell-workflow-authoring/10-UI-SPEC.md
@src/zeroth/graph/validation.py
@src/zeroth/graph/validation_errors.py
@src/zeroth/contracts/registry.py
@src/zeroth/studio/workflows_api.py
@src/zeroth/studio/sessions_api.py
@apps/studio/src/features/canvas/WorkflowCanvas.vue
@apps/studio/src/features/inspector/NodeInspector.vue

<interfaces>
From `src/zeroth/graph/validation_errors.py`:
```python
class ValidationIssue(BaseModel):
    severity: ValidationSeverity
    code: ValidationCode
    message: str
    graph_id: str
    node_id: str | None = None
    edge_id: str | None = None
    path: tuple[str, ...] = Field(default_factory=tuple)
    details: dict[str, Any] = Field(default_factory=dict)

class GraphValidationReport(BaseModel):
    graph_id: str
    issues: list[ValidationIssue] = Field(default_factory=list)
```

Contract lookup capability available from `src/zeroth/contracts/registry.py`:
```python
class ContractRegistry:
    def resolve(self, reference: ContractReference) -> ContractVersion: ...
    def resolve_model_type(self, reference: ContractReference) -> type[BaseModel]: ...
```

Draft-save contract to implement in this plan:
```json
PUT /studio/workflows/{workflow_id}/draft
{
  "lease_token": "uuid",
  "revision_token": "uuid",
  "graph": { ... full Graph document ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lease-protected draft save and validation/contract lookup APIs</name>
  <files>src/zeroth/studio/workflows/service.py, src/zeroth/studio/workflows_api.py, src/zeroth/studio/validation_api.py, src/zeroth/studio/app.py, tests/studio/test_validation_api.py</files>
  <read_first>.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md, .planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md, src/zeroth/graph/validation.py, src/zeroth/graph/validation_errors.py, src/zeroth/contracts/registry.py, src/zeroth/studio/workflows/service.py, src/zeroth/studio/workflows_api.py, src/zeroth/studio/app.py, tests/studio/test_validation_api.py</read_first>
  <behavior>
    - Test 1: `PUT /studio/workflows/{workflow_id}/draft` with the current `lease_token` and `revision_token` saves the full graph, rotates `revision_token`, and updates `last_saved_at`
    - Test 2: the same draft route returns `409` with detail `"revision token mismatch"` when the client submits a stale revision token and `409` with detail `"lease token required"` when the lease token is missing or wrong
    - Test 3: `POST /studio/workflows/{workflow_id}/validate` returns a serialized `GraphValidationReport` with `issues[*].node_id` and `issues[*].edge_id`, and `GET /studio/contracts/{contract_ref}` returns the latest contract schema for node-local editing
  </behavior>
  <action>Implement the save/validation boundary exactly. In `src/zeroth/studio/workflows/service.py`, add `update_draft(workflow_id: str, lease_token: str, revision_token: str, graph: Graph) -> WorkflowDetail` that refuses writes when the lease token is absent or stale, compares the submitted revision token to the current `workflow_draft_heads.revision_token`, saves the full graph through `GraphRepository.save`, sets `validation_status` to `unknown`, stamps `last_saved_at`, and rotates `revision_token` to a new UUID on success. In `src/zeroth/studio/workflows_api.py`, add `PUT /studio/workflows/{workflow_id}/draft` with a typed request body containing exactly `lease_token`, `revision_token`, and `graph`. In `src/zeroth/studio/validation_api.py`, add `POST /studio/workflows/{workflow_id}/validate` that loads the current draft graph and returns the exact `GraphValidationReport` model from `zeroth.graph.validation_errors`, plus `GET /studio/contracts/{contract_ref}` returning `{name, version, json_schema}` from `ContractRegistry.resolve(ContractReference(name=contract_ref, version=None))`. Register `validation_api` from `src/zeroth/studio/app.py`. Write `tests/studio/test_validation_api.py` covering success save, stale revision rejection, missing lease rejection, validation issue serialization, and contract lookup for an existing contract name.</action>
  <verify>
    <automated>uv run pytest tests/studio/test_validation_api.py -q</automated>
  </verify>
  <acceptance_criteria>`rg -n "@app.put\\(\"/studio/workflows/\\{workflow_id\\}/draft\"" src/zeroth/studio/workflows_api.py` matches once; `rg -n "@app.post\\(\"/studio/workflows/\\{workflow_id\\}/validate\"" src/zeroth/studio/validation_api.py` matches once; `rg -n "@app.get\\(\"/studio/contracts/\\{contract_ref\\}\"" src/zeroth/studio/validation_api.py` matches once; `rg -n "revision token mismatch" src/zeroth/studio/workflows/service.py src/zeroth/studio/workflows_api.py` matches at least once; `uv run pytest tests/studio/test_validation_api.py -q` exits 0.</acceptance_criteria>
  <done>Draft saves are guarded by lease and revision token checks, validation runs against the persisted draft, and contract schema lookups exist for node-local editor flows.</done>
</task>

<task type="auto">
  <name>Task 2: Add node-local validation and contract UX with structural autosave boundaries</name>
  <files>apps/studio/src/lib/api/studio.ts, apps/studio/src/lib/api/validation.ts, apps/studio/src/features/validation/ValidationSummary.vue, apps/studio/src/features/inspector/ContractPanel.vue, apps/studio/src/features/inspector/NodeInspector.vue, apps/studio/src/features/canvas/WorkflowCanvas.vue, apps/studio/src/stores/studioShell.ts</files>
  <read_first>.planning/phases/10-studio-shell-workflow-authoring/10-UI-SPEC.md, docs/superpowers/specs/2026-03-29-zeroth-studio-design.md, apps/studio/src/lib/api/studio.ts, apps/studio/src/features/inspector/NodeInspector.vue, apps/studio/src/features/canvas/WorkflowCanvas.vue, apps/studio/src/stores/studioShell.ts, src/zeroth/studio/workflows_api.py, src/zeroth/studio/validation_api.py</read_first>
  <action>Keep the editor minimal while making validation immediate and local. In `apps/studio/src/lib/api/studio.ts`, add `updateWorkflowDraft(workflowId, leaseToken, revisionToken, graph)` targeting `PUT /studio/workflows/{workflowId}/draft`. In `apps/studio/src/lib/api/validation.ts`, add `validateWorkflow(workflowId)` for `POST /studio/workflows/{workflowId}/validate` and `lookupContract(contractRef)` for `GET /studio/contracts/{contractRef}`. In `apps/studio/src/features/canvas/WorkflowCanvas.vue`, autosave only structural changes: node positions, node additions/removals, edge additions/removals, and edge reconnects must call `updateWorkflowDraft`; advanced workflow metadata and deployment/environment settings must not autosave in this plan. In `apps/studio/src/features/validation/ValidationSummary.vue`, render issue counts plus grouped node and edge issue lists using the backend `GraphValidationReport`. In `apps/studio/src/features/inspector/ContractPanel.vue`, render node-local contract inputs for `input_contract_ref` and `output_contract_ref`, fetch schemas via `lookupContract`, and display schema title/version inline instead of sending the user to a separate contracts page. In `apps/studio/src/features/inspector/NodeInspector.vue`, include the contract panel, compact recent activity, and local validation issues for the selected node only. Store `leaseToken`, `revisionToken`, selected node id, and autosave status in `apps/studio/src/stores/studioShell.ts`; after a successful autosave, update the revision token from the response so the next save is accepted.</action>
  <verify>
    <automated>npm --prefix apps/studio run build</automated>
  </verify>
  <acceptance_criteria>`rg -n "updateWorkflowDraft" apps/studio/src/lib/api/studio.ts apps/studio/src/features/canvas/WorkflowCanvas.vue` matches at least twice; `rg -n "validateWorkflow|lookupContract" apps/studio/src/lib/api/validation.ts apps/studio/src/features/inspector/NodeInspector.vue apps/studio/src/features/validation/ValidationSummary.vue` matches at least three times; `rg -n "input_contract_ref|output_contract_ref" apps/studio/src/features/inspector/ContractPanel.vue` matches both strings; `rg -n "autosaveStatus|revisionToken|leaseToken" apps/studio/src/stores/studioShell.ts` matches all three identifiers; `npm --prefix apps/studio run build` exits 0.</acceptance_criteria>
  <done>The frontend autosaves structural graph changes through the protected draft API, shows validation locally in the editor shell, and keeps contract configuration inside node-local inspector flows per AST-04.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/studio/test_validation_api.py -q` and `npm --prefix apps/studio run build`. Confirm the editor uses lease/revision guarded autosave and displays node-local contract/validation feedback without introducing a detached contracts surface.
</verification>

<success_criteria>
Phase 10 is functionally complete: draft workflow editing is separated from deployed graphs, the Studio shell exists, and node-local validation plus contract authoring operate inside a minimal canvas-first editor with explicit autosave boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/10-studio-shell-workflow-authoring/10-03-SUMMARY.md`
</output>
