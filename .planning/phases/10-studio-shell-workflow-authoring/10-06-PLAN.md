---
phase: 10-studio-shell-workflow-authoring
plan: 06
type: execute
wave: 5
depends_on: [10-04, 10-05]
files_modified:
  - apps/studio/src/lib/api/studio.ts
  - apps/studio/src/lib/api/validation.ts
  - apps/studio/src/features/validation/ValidationSummary.vue
  - apps/studio/src/features/inspector/ContractPanel.vue
  - apps/studio/src/features/inspector/NodeInspector.vue
  - apps/studio/src/features/canvas/WorkflowCanvas.vue
  - apps/studio/src/stores/studioShell.ts
  - apps/studio/src/stores/studioShell.test.ts
autonomous: true
requirements: [STU-02, AST-04, UX-01]
must_haves:
  truths:
    - "Autosave only runs after the editor acquires an active lease token for the selected workflow."
    - "The frontend renews the lease while the workflow stays open and releases it when the user leaves the workflow."
    - "Validation and contract feedback stay inside the node-local editor flow instead of sending the user to a detached contract library."
  artifacts:
    - path: "apps/studio/src/lib/api/validation.ts"
      provides: "frontend validation and contract lookup client"
    - path: "apps/studio/src/stores/studioShell.ts"
      provides: "lease token, revision token, autosave status, and heartbeat orchestration state"
    - path: "apps/studio/src/stores/studioShell.test.ts"
      provides: "proof that autosave is blocked without a current lease and resumed only after acquisition"
  key_links:
    - from: "apps/studio/src/features/canvas/WorkflowCanvas.vue"
      to: "apps/studio/src/lib/api/studio.ts"
      via: "canvas acquires lease, sends heartbeats, and saves draft updates only while a lease token is current"
      pattern: "acquireWorkflowLease|renewWorkflowLease|updateWorkflowDraft"
    - from: "apps/studio/src/features/inspector/NodeInspector.vue"
      to: "apps/studio/src/lib/api/validation.ts"
      via: "node-local validation and contract lookups"
      pattern: "validateWorkflow|lookupContract"
    - from: "apps/studio/src/lib/api/validation.ts"
      to: "/studio/contracts/{contract_ref:path}"
      via: "slash-safe contract lookup for refs like contract://input"
      pattern: "contract://input|encodeURIComponent"
---

<objective>
Add the lease-aware frontend authoring behaviors: acquire and maintain workflow leases, autosave structural edits only while a lease is active, and render node-local validation and contract UX.

Purpose: Finish the Phase 10 outcome by separating lease orchestration and node-local UX from backend API work, so the acceptance criteria can prove autosave never writes without an active lease token.
Output: Frontend lease lifecycle, protected autosave, local validation UI, contract inspector flows, and tests proving autosave only runs with a current lease.
</objective>

<execution_context>
@/Users/dondoe/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dondoe/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_notes>
- Read the Phase 10 section in `PROGRESS.md` before implementation starts.
- Use the `progress-logger` skill after every meaningful unit of work, including test passes/failures and any changed deliverable.
- Capture artifact paths under `phases/phase-10-studio-shell-workflow-authoring/artifacts/` and update `PROGRESS.md` before reporting completion.
</execution_notes>

<context>
@.planning/ROADMAP.md
@PROGRESS.md
@.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md
@.planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md
@.planning/phases/10-studio-shell-workflow-authoring/10-UI-SPEC.md
@apps/studio/src/lib/api/studio.ts
@apps/studio/src/stores/studioShell.ts
@src/zeroth/studio/workflows_api.py
@src/zeroth/studio/sessions_api.py
@src/zeroth/studio/validation_api.py

<interfaces>
Frontend write flow required in this plan:
```ts
await acquireWorkflowLease(workflowId)
await updateWorkflowDraft(workflowId, leaseToken, revisionToken, graph)
await renewWorkflowLease(workflowId, leaseToken)
await releaseWorkflowLease(workflowId, leaseToken)
```

Contract lookup must support slash refs:
```ts
await lookupContract("contract://input")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lease lifecycle orchestration, protected autosave, and node-local validation UX</name>
  <files>apps/studio/src/lib/api/studio.ts, apps/studio/src/lib/api/validation.ts, apps/studio/src/features/validation/ValidationSummary.vue, apps/studio/src/features/inspector/ContractPanel.vue, apps/studio/src/features/inspector/NodeInspector.vue, apps/studio/src/features/canvas/WorkflowCanvas.vue, apps/studio/src/stores/studioShell.ts, apps/studio/src/stores/studioShell.test.ts</files>
  <read_first>.planning/phases/10-studio-shell-workflow-authoring/10-UI-SPEC.md, docs/superpowers/specs/2026-03-29-zeroth-studio-design.md, apps/studio/src/lib/api/studio.ts, apps/studio/src/features/inspector/NodeInspector.vue, apps/studio/src/features/canvas/WorkflowCanvas.vue, apps/studio/src/stores/studioShell.ts, src/zeroth/studio/workflows_api.py, src/zeroth/studio/sessions_api.py, src/zeroth/studio/validation_api.py</read_first>
  <behavior>
    - Test 1: the store or orchestration layer does not call `updateWorkflowDraft` when `leaseToken` is missing, expired, or cleared after release
    - Test 2: selecting a workflow acquires a lease, stores `leaseToken`, schedules heartbeat renewal before expiry, and clears the token after route leave or workflow switch
    - Test 3: `lookupContract("contract://input")` reaches the slash-safe backend route, and node-local validation issues render only for the selected node inside the inspector
  </behavior>
  <action>Keep the editor minimal while making lease-gated autosave explicit. In `apps/studio/src/lib/api/studio.ts`, add `updateWorkflowDraft(workflowId, leaseToken, revisionToken, graph)` if it does not exist yet and ensure `acquireWorkflowLease`, `renewWorkflowLease`, and `releaseWorkflowLease` return typed lease payloads that include `lease_token` and `expires_at`. In `apps/studio/src/lib/api/validation.ts`, add `validateWorkflow(workflowId)` for `POST /studio/workflows/{workflowId}/validate` and `lookupContract(contractRef)` for the slash-safe contract route; construct the contract request so refs like `contract://input` survive path handling. In `apps/studio/src/stores/studioShell.ts`, store `leaseToken`, `leaseExpiresAt`, `revisionToken`, selected node ID, autosave status, and conflict state; add orchestration methods that acquire a lease when a workflow opens, renew it on a heartbeat interval before expiry, and release it on workflow switch, route leave, and `beforeunload`. In `apps/studio/src/features/canvas/WorkflowCanvas.vue`, autosave only structural changes and short-circuit autosave entirely unless the store reports an active lease token; after a successful save, update the revision token from the response. In `apps/studio/src/features/validation/ValidationSummary.vue`, render issue counts plus grouped node and edge issue lists using the backend `GraphValidationReport`. In `apps/studio/src/features/inspector/ContractPanel.vue`, render node-local contract inputs for `input_contract_ref` and `output_contract_ref`, fetch schemas via `lookupContract`, and display schema title or version inline. In `apps/studio/src/features/inspector/NodeInspector.vue`, include the contract panel, compact recent activity, and local validation issues for the selected node only. Add `apps/studio/src/stores/studioShell.test.ts` with Vitest-based tests proving autosave is blocked without an active lease, heartbeats run only after acquisition, release clears the token, and `lookupContract("contract://input")` is requested through the slash-safe route.</action>
  <verify>
    <automated>npm --prefix apps/studio run test -- --run</automated>
  </verify>
  <done>The frontend acquires, renews, and releases leases explicitly; autosave only runs while a current lease token exists; and validation plus contract configuration stay inside node-local inspector flows per AST-04.</done>
</task>

</tasks>

<verification>
Run `npm --prefix apps/studio run test -- --run` and confirm the lease orchestration tests prove autosave never calls the draft-save API without an active lease token, while node-local validation and contract flows stay inside the editor shell.
</verification>

<success_criteria>
Phase 10 is functionally complete: the Studio shell exists, backend draft editing is scope-checked, and the frontend only autosaves structural workflow edits after lease acquisition while surfacing validation and contracts in local editor flows.
</success_criteria>

<output>
After completion, create `.planning/phases/10-studio-shell-workflow-authoring/10-06-SUMMARY.md`
</output>
