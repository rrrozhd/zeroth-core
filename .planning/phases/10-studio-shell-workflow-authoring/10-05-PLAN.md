---
phase: 10-studio-shell-workflow-authoring
plan: 05
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - src/zeroth/studio/workflows/service.py
  - src/zeroth/studio/workflows_api.py
  - src/zeroth/studio/validation_api.py
  - src/zeroth/studio/app.py
  - tests/studio/test_validation_api.py
autonomous: true
requirements: [STU-02, AST-04]
must_haves:
  truths:
    - "Structural draft saves only succeed when the caller is in the owning tenant/workspace and supplies the active lease token plus current revision token."
    - "Authoring validation runs against the current workspace-scoped draft graph and returns normalized issues."
    - "Node-local contract lookup supports real refs containing slashes, such as `contract://input`."
  artifacts:
    - path: "src/zeroth/studio/workflows_api.py"
      provides: "lease-protected draft save route"
    - path: "src/zeroth/studio/validation_api.py"
      provides: "workspace-scoped validation and slash-safe contract lookup endpoints"
    - path: "tests/studio/test_validation_api.py"
      provides: "coverage for save conflicts, scope rejection, validation payloads, and contract refs with slashes"
  key_links:
    - from: "src/zeroth/studio/workflows_api.py"
      to: "src/zeroth/studio/workflows/service.py"
      via: "draft save route passes tenant/workspace scope, lease token, and revision token"
      pattern: "update_draft"
    - from: "src/zeroth/studio/validation_api.py"
      to: "src/zeroth/graph/validation.py"
      via: "GraphValidator.validate"
      pattern: "GraphValidator"
    - from: "src/zeroth/studio/validation_api.py"
      to: "src/zeroth/contracts/registry.py"
      via: "slash-safe contract lookup for node-local editor flows"
      pattern: "contract_ref:path|contract_ref"
---

<objective>
Add the backend authoring behaviors that make the shell usable: lease-protected draft save, backend-authoritative validation, and slash-safe node-local contract lookup.

Purpose: Separate backend save and validation work from the frontend autosave UX so the critical API contracts and scope rules are explicit and testable before the UI depends on them.
Output: Protected draft-save, validation, and contract lookup APIs with tests for workspace authorization, lease fencing, and contract refs like `contract://input`.
</objective>

<execution_context>
@/Users/dondoe/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dondoe/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_notes>
- Read the Phase 10 section in `PROGRESS.md` before implementation starts.
- Use the `progress-logger` skill after every meaningful unit of work, including test passes/failures and any changed deliverable.
- Capture artifact paths under `phases/phase-10-studio-shell-workflow-authoring/artifacts/` and update `PROGRESS.md` before reporting completion.
</execution_notes>

<context>
@.planning/ROADMAP.md
@PROGRESS.md
@.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md
@.planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md
@src/zeroth/graph/validation.py
@src/zeroth/graph/validation_errors.py
@src/zeroth/contracts/registry.py
@src/zeroth/studio/workflows_api.py
@src/zeroth/studio/sessions_api.py

<interfaces>
Draft-save contract to implement in this plan:
```json
PUT /studio/workflows/{workflow_id}/draft
{
  "lease_token": "uuid",
  "revision_token": "uuid",
  "graph": { "...": "full Graph document" }
}
```

Slash-safe contract lookup required in this plan:
```text
GET /studio/contracts/{contract_ref:path}
Example request: /studio/contracts/contract://input
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scope-aware draft save and validation and slash-safe contract lookup APIs</name>
  <files>src/zeroth/studio/workflows/service.py, src/zeroth/studio/workflows_api.py, src/zeroth/studio/validation_api.py, src/zeroth/studio/app.py, tests/studio/test_validation_api.py</files>
  <read_first>.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md, .planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md, src/zeroth/graph/validation.py, src/zeroth/graph/validation_errors.py, src/zeroth/contracts/registry.py, src/zeroth/studio/workflows/service.py, src/zeroth/studio/workflows_api.py, src/zeroth/studio/app.py, tests/studio/test_validation_api.py</read_first>
  <behavior>
    - Test 1: `PUT /studio/workflows/{workflow_id}/draft` with the current `lease_token` and `revision_token` from the owning workspace saves the full graph, rotates `revision_token`, and updates `last_saved_at`
    - Test 2: the same route returns `409` with detail `"revision token mismatch"` for a stale token, `409` with detail `"lease token required"` when the lease token is missing or wrong, and `404` when a caller from a different workspace attempts the save
    - Test 3: `POST /studio/workflows/{workflow_id}/validate` returns a serialized `GraphValidationReport`, and `GET /studio/contracts/{contract_ref:path}` resolves a real slash-containing ref such as `contract://input`
  </behavior>
  <action>Implement the save and validation boundary exactly. In `src/zeroth/studio/workflows/service.py`, add `update_draft(tenant_id: str, workspace_id: str, workflow_id: str, lease_token: str, revision_token: str, graph: Graph) -> WorkflowDetail` that refuses writes when the workflow is outside the caller scope, when the lease token is absent or stale, or when the submitted revision token differs from the current `workflow_draft_heads.revision_token`; on success, save the full graph through `GraphRepository`, set `validation_status` to `unknown`, stamp `last_saved_at`, and rotate `revision_token` to a new UUID. In `src/zeroth/studio/workflows_api.py`, add `PUT /studio/workflows/{workflow_id}/draft` with a typed request body containing exactly `lease_token`, `revision_token`, and `graph`; resolve tenant/workspace scope from the authenticated Studio principal, not from client input. In `src/zeroth/studio/validation_api.py`, add `POST /studio/workflows/{workflow_id}/validate` that loads the current draft graph inside the caller scope and returns the exact `GraphValidationReport` model from `zeroth.graph.validation_errors`, plus `GET /studio/contracts/{contract_ref:path}` returning `{name, version, json_schema}` from `ContractRegistry.resolve(ContractReference(name=contract_ref, version=None))`. Keep the contract route slash-safe with the FastAPI `{contract_ref:path}` converter, and use `contract://input` in tests instead of a simple slug. Register `validation_api` from `src/zeroth/studio/app.py`. Write `tests/studio/test_validation_api.py` covering successful save, stale revision rejection, missing lease rejection, wrong-workspace rejection, validation issue serialization, and contract lookup for `contract://input`.</action>
  <verify>
    <automated>uv run pytest tests/studio/test_validation_api.py -q</automated>
  </verify>
  <done>Draft saves are guarded by scope, lease, and revision token checks; validation runs against the persisted draft; and contract schema lookups support slash-containing refs for node-local editor flows.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/studio/test_validation_api.py -q` and confirm draft saves are scope-checked and lease-checked, validation runs against the persisted draft, and contract lookups work for `contract://input`.
</verification>

<success_criteria>
The backend authoring API now protects draft writes with scope and lease fencing, returns normalized validation output for authoring flows, and exposes a slash-safe contract lookup route that later frontend work can call directly.
</success_criteria>

<output>
After completion, create `.planning/phases/10-studio-shell-workflow-authoring/10-05-SUMMARY.md`
</output>
