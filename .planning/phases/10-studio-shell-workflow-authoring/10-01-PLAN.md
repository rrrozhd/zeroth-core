---
phase: 10-studio-shell-workflow-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/zeroth/studio/__init__.py
  - src/zeroth/studio/models.py
  - src/zeroth/studio/bootstrap.py
  - src/zeroth/studio/app.py
  - src/zeroth/studio/workflows_api.py
  - src/zeroth/studio/sessions_api.py
  - src/zeroth/studio/workflows/repository.py
  - src/zeroth/studio/workflows/service.py
  - src/zeroth/studio/leases/models.py
  - src/zeroth/studio/leases/repository.py
  - src/zeroth/studio/leases/service.py
  - tests/studio/test_workflows_repository.py
  - tests/studio/test_studio_app.py
autonomous: true
requirements: [STU-02]
must_haves:
  truths:
    - "Studio has a workspace-scoped backend separate from the deployment-bound service wrapper."
    - "A workflow draft can be listed and loaded without touching deployed runtime snapshots."
    - "Draft mutation ownership is represented by an edit lease rather than only by auth state."
  artifacts:
    - path: "src/zeroth/studio/bootstrap.py"
      provides: "StudioBootstrap container and bootstrap_studio entry point"
    - path: "src/zeroth/studio/workflows/repository.py"
      provides: "workflow_records and workflow_draft_heads persistence"
    - path: "src/zeroth/studio/sessions_api.py"
      provides: "lease acquire, renew, and release HTTP routes"
  key_links:
    - from: "src/zeroth/studio/bootstrap.py"
      to: "src/zeroth/studio/app.py"
      via: "create_studio_app(app.state.bootstrap)"
      pattern: "app\\.state\\.bootstrap = bootstrap"
    - from: "src/zeroth/studio/workflows_api.py"
      to: "src/zeroth/studio/workflows/service.py"
      via: "workflow list/create/load route handlers"
      pattern: "workflow_service\\."
    - from: "src/zeroth/studio/sessions_api.py"
      to: "src/zeroth/studio/leases/service.py"
      via: "lease acquire/renew/release handlers"
      pattern: "lease_service\\."
---

<objective>
Create the Studio authoring control plane on the backend: workflow metadata, draft-head lookup, and edit leases with a dedicated FastAPI app instead of extending the deployment-scoped wrapper.

Purpose: Preserve the locked draft-vs-runtime boundary from STU-02 while keeping Phase 10 aligned with the existing bootstrap/repository patterns.
Output: A new `zeroth.studio` package with workflow listing/loading APIs, lease APIs, and tests proving the workspace-scoped app boots independently.
</objective>

<execution_context>
@/Users/dondoe/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dondoe/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md
@.planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md
@src/zeroth/service/app.py
@src/zeroth/service/bootstrap.py
@src/zeroth/graph/repository.py
@src/zeroth/storage/sqlite.py
@tests/service/test_app.py

<interfaces>
From `src/zeroth/service/app.py`:
```python
class ServiceBootstrapLike(Protocol):
    deployment: object
    graph: object
    contract_registry: object
    approval_service: object
    run_repository: object
    orchestrator: object
    audit_repository: object
    authenticator: object

def create_app(bootstrap: ServiceBootstrapLike) -> FastAPI: ...
```

From `src/zeroth/service/bootstrap.py`:
```python
@dataclass(slots=True)
class ServiceBootstrap:
    deployment_service: DeploymentService
    deployment: Deployment
    graph: Graph
    run_repository: RunRepository
    thread_repository: ThreadRepository
    approval_service: ApprovalService
    audit_repository: AuditRepository
    contract_registry: ContractRegistry
    orchestrator: RuntimeOrchestrator
    auth_config: ServiceAuthConfig
    authenticator: ServiceAuthenticator
```

From `src/zeroth/graph/repository.py`:
```python
class GraphRepository:
    def create(self, graph: Graph) -> Graph: ...
    def get(self, graph_id: str, version: int | None = None) -> Graph | None: ...
    def list(self) -> list[Graph]: ...
    def publish(self, graph_id: str, version: int | None = None) -> Graph: ...
    def clone_published_to_draft(self, graph_id: str, version: int | None = None) -> Graph: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create workflow metadata and lease persistence contracts</name>
  <files>src/zeroth/studio/models.py, src/zeroth/studio/workflows/repository.py, src/zeroth/studio/workflows/service.py, src/zeroth/studio/leases/models.py, src/zeroth/studio/leases/repository.py, src/zeroth/studio/leases/service.py, tests/studio/test_workflows_repository.py</files>
  <read_first>.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md, .planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md, src/zeroth/graph/repository.py, src/zeroth/storage/sqlite.py, tests/service/test_app.py, src/zeroth/studio/models.py, src/zeroth/studio/workflows/repository.py, src/zeroth/studio/workflows/service.py, src/zeroth/studio/leases/models.py, src/zeroth/studio/leases/repository.py, src/zeroth/studio/leases/service.py, tests/studio/test_workflows_repository.py</read_first>
  <behavior>
    - Test 1: creating a workflow record stores `workflow_id`, `graph_id`, `name`, `folder_path`, `draft_graph_version`, and `validation_status="unknown"` in Studio-owned tables while leaving graph content in `graph_versions`
    - Test 2: loading a workflow by `workflow_id` returns the current draft graph and does not require a deployment record
    - Test 3: acquiring a lease for the same workflow from a second subject before expiry returns a conflict outcome; renewing with the original lease token extends `expires_at`
  </behavior>
  <action>Implement the Studio persistence boundary per STU-02 by adding a new `src/zeroth/studio/` package. In `src/zeroth/studio/models.py`, define Pydantic models for `WorkflowRecord`, `WorkflowDraftHead`, `WorkflowSummary`, `WorkflowDetail`, `WorkflowLease`, and `LeaseConflictError`-friendly payloads with `ConfigDict(extra="forbid")`. In `src/zeroth/studio/workflows/repository.py`, add `STUDIO_WORKFLOW_SCHEMA_SCOPE = "studio_workflows"` migrations that create exactly these tables: `workflow_records(workflow_id TEXT PRIMARY KEY, graph_id TEXT NOT NULL, name TEXT NOT NULL, folder_path TEXT NOT NULL DEFAULT '/', archived_at TEXT, created_at TEXT NOT NULL, updated_at TEXT NOT NULL)`, `workflow_draft_heads(workflow_id TEXT PRIMARY KEY REFERENCES workflow_records(workflow_id) ON DELETE CASCADE, draft_graph_version INTEGER NOT NULL, revision_token TEXT NOT NULL, validation_status TEXT NOT NULL, last_saved_at TEXT NOT NULL)`. Store graph documents only through `GraphRepository`; Studio tables wrap graph lineage, they do not duplicate the serialized graph. In `src/zeroth/studio/leases/repository.py`, add `STUDIO_LEASE_SCHEMA_SCOPE = "studio_leases"` migrations that create `workflow_leases(workflow_id TEXT PRIMARY KEY REFERENCES workflow_records(workflow_id) ON DELETE CASCADE, lease_token TEXT NOT NULL, subject TEXT NOT NULL, workspace_id TEXT, acquired_at TEXT NOT NULL, expires_at TEXT NOT NULL)`. In `src/zeroth/studio/workflows/service.py`, implement `list_workflows()`, `create_workflow(name: str, folder_path: str = "/")`, and `get_workflow(workflow_id: str)`; `create_workflow` must create a draft `Graph` with `version=1`, `status=GraphStatus.DRAFT`, `entry_step=""`, `nodes=[]`, `edges=[]`, `graph_id=f"workflow:{workflow_id}"`, and initialize `revision_token` with a new UUID. In `src/zeroth/studio/leases/service.py`, implement `acquire_lease(workflow_id, subject, workspace_id, ttl_seconds=30)`, `renew_lease(workflow_id, lease_token, ttl_seconds=30)`, and `release_lease(workflow_id, lease_token)` with conflict detection based on unexpired rows. Write repository/service tests in `tests/studio/test_workflows_repository.py` that exercise the exact table fields, graph linkage, and lease conflict rules.</action>
  <verify>
    <automated>uv run pytest tests/studio/test_workflows_repository.py -q</automated>
  </verify>
  <acceptance_criteria>`rg -n "CREATE TABLE IF NOT EXISTS workflow_records" src/zeroth/studio/workflows/repository.py` matches once; `rg -n "CREATE TABLE IF NOT EXISTS workflow_draft_heads" src/zeroth/studio/workflows/repository.py` matches once; `rg -n "CREATE TABLE IF NOT EXISTS workflow_leases" src/zeroth/studio/leases/repository.py` matches once; `rg -n "def acquire_lease\\(" src/zeroth/studio/leases/service.py` matches once; `uv run pytest tests/studio/test_workflows_repository.py -q` exits 0.</acceptance_criteria>
  <done>Studio-owned workflow metadata and lease repositories exist, a new workflow creates a draft graph head with a revision token, and lease conflicts are enforced in automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose Studio bootstrap and HTTP APIs for workflow list/load and lease lifecycle</name>
  <files>src/zeroth/studio/__init__.py, src/zeroth/studio/bootstrap.py, src/zeroth/studio/app.py, src/zeroth/studio/workflows_api.py, src/zeroth/studio/sessions_api.py, tests/studio/test_studio_app.py</files>
  <read_first>.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md, .planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md, src/zeroth/service/app.py, src/zeroth/service/bootstrap.py, src/zeroth/service/auth.py, tests/service/helpers.py, src/zeroth/studio/bootstrap.py, src/zeroth/studio/app.py, src/zeroth/studio/workflows_api.py, src/zeroth/studio/sessions_api.py, tests/studio/test_studio_app.py</read_first>
  <behavior>
    - Test 1: `GET /studio/workflows` returns `{"items": [...]}` with `workflow_id`, `name`, `folder_path`, `draft_graph_version`, `validation_status`, and `lease_owner_subject`
    - Test 2: `POST /studio/workflows` accepts `{"name":"New Workflow","folder_path":"/sales"}` and returns `201` with a created draft head and empty graph document
    - Test 3: `POST /studio/workflows/{workflow_id}/leases`, `POST /studio/workflows/{workflow_id}/leases/ping`, and `DELETE /studio/workflows/{workflow_id}/leases/{lease_token}` enforce lease ownership and return `409` for a conflicting active lease
  </behavior>
  <action>Follow the existing FastAPI bootstrap pattern but create a dedicated authoring app per the locked delivery decision. In `src/zeroth/studio/bootstrap.py`, define `StudioBootstrap` with fields `workflow_service`, `lease_service`, `graph_repository`, `contract_registry`, `auth_config`, and `authenticator`, plus `bootstrap_studio(database: SQLiteDatabase, auth_config: ServiceAuthConfig | None = None, bearer_token_verifier: JWTBearerTokenVerifier | None = None) -> StudioBootstrap`. In `src/zeroth/studio/app.py`, create `create_studio_app(bootstrap: StudioBootstrapLike) -> FastAPI`, add the same auth middleware pattern used by `src/zeroth/service/app.py`, and mount Studio routes under `/studio`. In `src/zeroth/studio/workflows_api.py`, register `GET /studio/workflows`, `POST /studio/workflows`, and `GET /studio/workflows/{workflow_id}` using typed request/response models with `ConfigDict(extra="forbid")`; responses must include the current draft graph and `revision_token` on detail loads. In `src/zeroth/studio/sessions_api.py`, register `POST /studio/workflows/{workflow_id}/leases`, `POST /studio/workflows/{workflow_id}/leases/ping`, and `DELETE /studio/workflows/{workflow_id}/leases/{lease_token}`. Use the existing service auth system from `zeroth.service.auth` and return `409` with detail `"active lease held by another editor"` on conflicts. Export `create_studio_app`, `bootstrap_studio`, and `StudioBootstrap` from `src/zeroth/studio/__init__.py`. Add `tests/studio/test_studio_app.py` using `fastapi.testclient.TestClient` and `tests/service/helpers.default_service_auth_config()` to prove the app boots, routes serialize expected fields, and lease conflicts are surfaced over HTTP.</action>
  <verify>
    <automated>uv run pytest tests/studio/test_studio_app.py -q</automated>
  </verify>
  <acceptance_criteria>`rg -n "def bootstrap_studio\\(" src/zeroth/studio/bootstrap.py` matches once; `rg -n "def create_studio_app\\(" src/zeroth/studio/app.py` matches once; `rg -n "@app.get\\(\"/studio/workflows\"" src/zeroth/studio/workflows_api.py` matches once; `rg -n "@app.post\\(\"/studio/workflows/\\{workflow_id\\}/leases\"" src/zeroth/studio/sessions_api.py` matches once; `uv run pytest tests/studio/test_studio_app.py -q` exits 0.</acceptance_criteria>
  <done>The repo has a new Studio FastAPI app with workflow list/create/detail routes, lease lifecycle routes, and tests proving the authoring app is separate from the deployment wrapper.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/studio/test_workflows_repository.py tests/studio/test_studio_app.py -q` and confirm the new Studio package exposes the workflow and lease APIs without changing the deployment-scoped service bootstrap.
</verification>

<success_criteria>
Phase 10 backend foundations exist: a dedicated `zeroth.studio` package boots independently, workflow drafts are represented as mutable graph heads separate from deployments, and edit leases are enforced through typed APIs and automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/10-studio-shell-workflow-authoring/10-01-SUMMARY.md`
</output>
