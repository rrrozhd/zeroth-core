---
phase: 10-studio-shell-workflow-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/zeroth/studio/__init__.py
  - src/zeroth/studio/models.py
  - src/zeroth/studio/workflows/repository.py
  - src/zeroth/studio/workflows/service.py
  - src/zeroth/studio/leases/models.py
  - src/zeroth/studio/leases/repository.py
  - src/zeroth/studio/leases/service.py
  - tests/studio/test_workflows_repository.py
autonomous: true
requirements: [STU-02]
must_haves:
  truths:
    - "A workspace-scoped user can create a workflow draft and persist its metadata without creating or selecting a deployment."
    - "A workflow draft only loads inside the tenant and workspace that own it."
    - "A second editor without the active lease for that same workspace-scoped draft receives a conflict instead of silently taking over."
  artifacts:
    - path: "src/zeroth/studio/workflows/repository.py"
      provides: "workspace-owned workflow_records and workflow_draft_heads persistence"
    - path: "src/zeroth/studio/workflows/service.py"
      provides: "scope-aware workflow create, list, and lookup behavior"
    - path: "src/zeroth/studio/leases/service.py"
      provides: "workspace-aware lease acquisition, renewal, release, and conflict enforcement"
  key_links:
    - from: "src/zeroth/studio/workflows/repository.py"
      to: "src/zeroth/studio/workflows/service.py"
      via: "service reads and writes workflow metadata plus draft head state through tenant/workspace filters"
      pattern: "WorkflowRepository"
    - from: "src/zeroth/studio/leases/repository.py"
      to: "src/zeroth/studio/leases/service.py"
      via: "lease service enforces unexpired lease ownership within the same tenant/workspace"
      pattern: "LeaseRepository"
---

<objective>
Create the Studio authoring data foundation on the backend: workspace-owned workflow metadata, draft-head lookup, and edit leases, without yet wiring the FastAPI app surface.

Purpose: Preserve the locked draft-vs-runtime boundary from STU-02 while making tenant/workspace ownership explicit in the storage model instead of deferring it to later route wiring.
Output: A new `zeroth.studio` package with scope-aware workflow and lease models, repositories, services, and tests proving workspace isolation before HTTP endpoints are added.
</objective>

<execution_context>
@/Users/dondoe/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dondoe/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_notes>
- Read the Phase 10 section in `PROGRESS.md` before implementation starts.
- Use the `progress-logger` skill after every meaningful unit of work, including test passes/failures and any changed deliverable.
- Capture artifact paths under `phases/phase-10-studio-shell-workflow-authoring/artifacts/` and update `PROGRESS.md` before reporting completion.
</execution_notes>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@PROGRESS.md
@.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md
@.planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md
@src/zeroth/graph/repository.py
@src/zeroth/storage/sqlite.py
@tests/service/helpers.py

<interfaces>
Studio ownership fields that must exist on persisted resources in this plan:
```python
class WorkflowRecord(BaseModel):
    workflow_id: str
    tenant_id: str
    workspace_id: str
    graph_id: str
    name: str
    folder_path: str

class WorkflowDraftHead(BaseModel):
    workflow_id: str
    tenant_id: str
    workspace_id: str
    draft_graph_version: int
    revision_token: str
    validation_status: str

class WorkflowLease(BaseModel):
    workflow_id: str
    tenant_id: str
    workspace_id: str
    lease_token: str
    subject: str
```

Use scoped test helpers from `tests/service/helpers.py`:
```python
def scoped_auth_config(
    *credentials: tuple[str, str, ServiceRole, str, str | None],
) -> ServiceAuthConfig: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create workspace-owned workflow metadata, draft-head, and lease persistence foundations</name>
  <files>src/zeroth/studio/__init__.py, src/zeroth/studio/models.py, src/zeroth/studio/workflows/repository.py, src/zeroth/studio/workflows/service.py, src/zeroth/studio/leases/models.py, src/zeroth/studio/leases/repository.py, src/zeroth/studio/leases/service.py, tests/studio/test_workflows_repository.py</files>
  <read_first>.planning/phases/10-studio-shell-workflow-authoring/10-CONTEXT.md, .planning/phases/10-studio-shell-workflow-authoring/10-RESEARCH.md, src/zeroth/graph/repository.py, src/zeroth/storage/sqlite.py, tests/service/helpers.py, src/zeroth/studio/models.py, src/zeroth/studio/workflows/repository.py, src/zeroth/studio/workflows/service.py, src/zeroth/studio/leases/models.py, src/zeroth/studio/leases/repository.py, src/zeroth/studio/leases/service.py, tests/studio/test_workflows_repository.py</read_first>
  <behavior>
    - Test 1: creating a workflow record stores `workflow_id`, `tenant_id`, `workspace_id`, `graph_id`, `name`, `folder_path`, `draft_graph_version`, and `validation_status="unknown"` in Studio-owned tables while leaving graph content in `graph_versions`
    - Test 2: listing or loading a workflow with the wrong tenant or workspace returns no result, while the owning tenant/workspace can still load the current draft graph without a deployment record
    - Test 3: acquiring a lease for the same workflow from a second subject before expiry returns a conflict outcome; renewing or releasing with the wrong workspace or token fails, while the original scoped lease token extends `expires_at`
  </behavior>
  <action>Implement the Studio persistence boundary per STU-02 with explicit workspace ownership. In `src/zeroth/studio/models.py`, define Pydantic models for `WorkflowRecord`, `WorkflowDraftHead`, `WorkflowSummary`, `WorkflowDetail`, `WorkflowLease`, and conflict payloads using `ConfigDict(extra="forbid")`; all workflow and lease resources must include concrete `tenant_id` and `workspace_id` fields. In `src/zeroth/studio/workflows/repository.py`, add `STUDIO_WORKFLOW_SCHEMA_SCOPE = "studio_workflows"` migrations that create exactly these tables: `workflow_records(workflow_id TEXT PRIMARY KEY, tenant_id TEXT NOT NULL, workspace_id TEXT NOT NULL, graph_id TEXT NOT NULL, name TEXT NOT NULL, folder_path TEXT NOT NULL DEFAULT '/', archived_at TEXT, created_at TEXT NOT NULL, updated_at TEXT NOT NULL)`, plus an index on `(tenant_id, workspace_id, folder_path, name)`; `workflow_draft_heads(workflow_id TEXT PRIMARY KEY REFERENCES workflow_records(workflow_id) ON DELETE CASCADE, tenant_id TEXT NOT NULL, workspace_id TEXT NOT NULL, draft_graph_version INTEGER NOT NULL, revision_token TEXT NOT NULL, validation_status TEXT NOT NULL, last_saved_at TEXT NOT NULL)`. In `src/zeroth/studio/leases/repository.py`, create `workflow_leases(workflow_id TEXT PRIMARY KEY REFERENCES workflow_records(workflow_id) ON DELETE CASCADE, tenant_id TEXT NOT NULL, workspace_id TEXT NOT NULL, lease_token TEXT NOT NULL, subject TEXT NOT NULL, acquired_at TEXT NOT NULL, expires_at TEXT NOT NULL)`. Keep serialized graph documents only in `GraphRepository`; Studio tables wrap ownership, draft-head lineage, and lease state, they do not duplicate graph JSON. In `src/zeroth/studio/workflows/service.py`, implement `list_workflows(tenant_id: str, workspace_id: str)`, `create_workflow(tenant_id: str, workspace_id: str, name: str, folder_path: str = "/")`, and `get_workflow(tenant_id: str, workspace_id: str, workflow_id: str)` so every read/write is filtered by scope. In `src/zeroth/studio/leases/service.py`, implement `acquire_lease(workflow_id, tenant_id, workspace_id, subject, ttl_seconds=30)`, `renew_lease(workflow_id, tenant_id, workspace_id, lease_token, ttl_seconds=30)`, and `release_lease(workflow_id, tenant_id, workspace_id, lease_token)` with conflict detection based on unexpired rows in the same tenant/workspace only. Write `tests/studio/test_workflows_repository.py` to prove exact schema fields, graph linkage, workspace isolation, and lease conflicts across two workspaces.</action>
  <verify>
    <automated>uv run pytest tests/studio/test_workflows_repository.py -q</automated>
  </verify>
  <done>Studio-owned workflow metadata and lease repositories exist with concrete tenant/workspace ownership fields, workflow loads are scope-filtered, and lease conflicts are enforced in automated tests.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/studio/test_workflows_repository.py -q` and confirm the new Studio package persists workflow metadata, draft heads, and leases with tenant/workspace ownership and isolation before any HTTP wiring is added.
</verification>

<success_criteria>
Phase 10 backend data foundations exist: a dedicated `zeroth.studio` package persists workspace-owned workflow drafts separately from deployments, returns revision-token-bearing draft details only inside the owning scope, and enforces edit leases in automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/10-studio-shell-workflow-authoring/10-01-SUMMARY.md`
</output>
