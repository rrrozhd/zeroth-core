---
phase: 27-ship-zeroth-as-pip-installable-library-zeroth-core
plan: 03
type: execute
wave: 3
depends_on: ["27-02"]
files_modified:
  - scripts/rename_to_zeroth_core.py
  - scripts/rewrite_zeroth_refs.sh
  - pyproject.toml
  - Dockerfile
  - docker-compose.yml
  - alembic.ini
  - src/zeroth
  - tests
autonomous: true
requirements: [RENAME-01, RENAME-02, RENAME-03]

must_haves:
  truths:
    - "All 27 first-level Python subpackages move under `src/zeroth/core/` via `git mv`"
    - "`src/zeroth/__init__.py` is removed and `src/zeroth/core/__init__.py` exists"
    - "Python imports and `importlib.import_module()` string arguments point at `zeroth.core.*`"
    - "`pyproject.toml` declares `name = \"zeroth-core\"` and wheel packages `['src/zeroth/core']`"
    - "Runtime path strings such as the Docker entrypoint and Alembic script location point at the renamed package layout"
  artifacts:
    - path: "scripts/rename_to_zeroth_core.py"
      provides: "Format-preserving libcst codemod for Python imports"
      contains: "libcst"
    - path: "scripts/rewrite_zeroth_refs.sh"
      provides: "Explicit rewrite pass for non-Python path strings"
      contains: "Dockerfile"
    - path: "pyproject.toml"
      provides: "Namespace-package packaging metadata for zeroth-core"
      contains: "name = \"zeroth-core\""
    - path: "Dockerfile"
      provides: "Updated runtime module entrypoint"
      contains: "zeroth.core.service.entrypoint"
  key_links:
    - from: "scripts/rename_to_zeroth_core.py"
      to: "tests"
      via: "codemod rewrites test imports from `zeroth.*` to `zeroth.core.*`"
      pattern: "zeroth.core"
    - from: "pyproject.toml"
      to: "src/zeroth/core/__init__.py"
      via: "wheel target and importable package root must agree"
      pattern: "src/zeroth/core"
---

<objective>
Relocate the entire Python source tree into `zeroth.core.*`, rewrite all import/module-path references, and switch packaging metadata to the new namespace layout without changing runtime behavior.

Purpose: this is the core rename step of Phase 27.
Output: renamed source tree, codemod + text-rewrite automation, and packaging metadata aligned with `zeroth-core`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/27-CONTEXT.md
@.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/27-RESEARCH.md
@.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/27-01-PLAN.md
@pyproject.toml
@Dockerfile
@docker-compose.yml
@alembic.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rename automation and move every top-level package into `src/zeroth/core/`</name>
  <files>
    scripts/rename_to_zeroth_core.py,
    src/zeroth,
    pyproject.toml
  </files>
  <read_first>
    - .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/27-CONTEXT.md
    - .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/27-RESEARCH.md
    - pyproject.toml
    - src/zeroth/__init__.py
  </read_first>
  <action>
Add `libcst` to `[dependency-groups].dev` in `pyproject.toml` if it is missing.

Create `scripts/rename_to_zeroth_core.py` that:
- imports `libcst`
- walks `.py` files under `src/`, `tests/`, `apps/`, and `scripts/`
- rewrites these exact patterns:
  - `import zeroth` -> `import zeroth.core`
  - `import zeroth.X` -> `import zeroth.core.X`
  - `from zeroth import X` -> `from zeroth.core import X`
  - `from zeroth.X import Y` -> `from zeroth.core.X import Y`
  - `importlib.import_module("zeroth.X")` -> `importlib.import_module("zeroth.core.X")`

Then create `src/zeroth/core/` and move these exact directories with `git mv`:
- `src/zeroth/agent_runtime`
- `src/zeroth/approvals`
- `src/zeroth/audit`
- `src/zeroth/conditions`
- `src/zeroth/config`
- `src/zeroth/contracts`
- `src/zeroth/demos`
- `src/zeroth/deployments`
- `src/zeroth/dispatch`
- `src/zeroth/econ`
- `src/zeroth/execution_units`
- `src/zeroth/graph`
- `src/zeroth/guardrails`
- `src/zeroth/identity`
- `src/zeroth/mappings`
- `src/zeroth/memory`
- `src/zeroth/migrations`
- `src/zeroth/observability`
- `src/zeroth/orchestrator`
- `src/zeroth/policy`
- `src/zeroth/runs`
- `src/zeroth/sandbox_sidecar`
- `src/zeroth/secrets`
- `src/zeroth/service`
- `src/zeroth/storage`
- `src/zeroth/studio`
- `src/zeroth/webhooks`

Create `src/zeroth/core/__init__.py` and remove `src/zeroth/__init__.py` in the same execution step.
  </action>
  <verify>
    <automated>uv run python scripts/rename_to_zeroth_core.py --help</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/rename_to_zeroth_core.py` exists
    - `scripts/rename_to_zeroth_core.py` contains `import libcst`
    - `scripts/rename_to_zeroth_core.py` contains `importlib.import_module`
    - `src/zeroth/core/__init__.py` exists after execution
    - `test ! -f src/zeroth/__init__.py` succeeds after execution
    - `find src/zeroth -maxdepth 1 -mindepth 1 | sort` contains `src/zeroth/core`
  </acceptance_criteria>
  <done>Rename automation exists and the source tree is relocated into `src/zeroth/core/` with PEP 420 enabled at the top level</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite non-Python references and packaging metadata to the new namespace</name>
  <files>
    scripts/rewrite_zeroth_refs.sh,
    pyproject.toml,
    Dockerfile,
    docker-compose.yml,
    alembic.ini
  </files>
  <read_first>
    - pyproject.toml
    - Dockerfile
    - docker-compose.yml
    - alembic.ini
    - scripts/rename_to_zeroth_core.py
  </read_first>
  <action>
Create `scripts/rewrite_zeroth_refs.sh` as a checked-in Bash script with `set -euo pipefail` that rewrites these exact values:
- in `pyproject.toml`: `name = "zeroth"` -> `name = "zeroth-core"`
- in `pyproject.toml`: `[tool.hatch.build.targets.wheel] packages = ["src/zeroth"]` -> `packages = ["src/zeroth/core"]`
- in `Dockerfile`: `python -m zeroth.service.entrypoint` -> `python -m zeroth.core.service.entrypoint`
- in `alembic.ini`: `script_location = src/zeroth/migrations` -> `script_location = src/zeroth/core/migrations`
- in any text file under repo root matching `Dockerfile`, `docker-compose.yml`, `README.md`, `*.md`, `*.yml`, `*.yaml`, `*.toml`: replace module-path strings `zeroth.` with `zeroth.core.` only where they refer to Python import/module paths

Run:
- `uv sync`
- `uv run python scripts/rename_to_zeroth_core.py`
- `bash scripts/rewrite_zeroth_refs.sh`

Do not change the `econ-instrumentation-sdk @ file:///Users/dondoe/coding/regulus/sdk/python` dependency.
  </action>
  <verify>
    <automated>bash -n scripts/rewrite_zeroth_refs.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/rewrite_zeroth_refs.sh` exists
    - `scripts/rewrite_zeroth_refs.sh` contains `name = \"zeroth-core\"`
    - `pyproject.toml` contains `name = "zeroth-core"`
    - `pyproject.toml` contains `packages = ["src/zeroth/core"]`
    - `Dockerfile` contains `zeroth.core.service.entrypoint`
    - `alembic.ini` contains `src/zeroth/core/migrations`
    - `bash -n scripts/rewrite_zeroth_refs.sh` exits 0
  </acceptance_criteria>
  <done>Packaging metadata and non-Python path references are aligned with the renamed `zeroth.core.*` layout</done>
</task>

<task type="auto">
  <name>Task 3: Prove namespace-package semantics and remove stale `zeroth.*` imports</name>
  <files>
    src/zeroth,
    tests,
    pyproject.toml
  </files>
  <read_first>
    - scripts/rename_to_zeroth_core.py
    - scripts/rewrite_zeroth_refs.sh
    - pyproject.toml
  </read_first>
  <action>
Run these exact smoke checks after the rename:
- `test ! -f src/zeroth/__init__.py`
- `uv run python -c "import zeroth.core; print(zeroth.core.__path__)"`
- `uv run python -c "import zeroth; print(zeroth.__path__); print(hasattr(zeroth, '__file__'))"`
- `uv run python -c "from zeroth.core.orchestrator import RuntimeOrchestrator"`
- `rg -nP '\\bfrom zeroth(?!\\.core)\\b|\\bimport zeroth(?!\\.core)\\b' src tests apps scripts`

If the `rg` command finds any stale import, keep iterating on the codemod and rewrite script until the command exits non-zero.
  </action>
  <verify>
    <automated>uv run python -c "import zeroth.core; print(zeroth.core.__path__)"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/zeroth/__init__.py` succeeds
    - `uv run python -c "import zeroth.core; print(zeroth.core.__path__)"` exits 0
    - `uv run python -c "import zeroth; print(zeroth.__path__); print(hasattr(zeroth, '__file__'))"` exits 0
    - `uv run python -c "from zeroth.core.orchestrator import RuntimeOrchestrator"` exits 0
    - `rg -nP '\\bfrom zeroth(?!\\.core)\\b|\\bimport zeroth(?!\\.core)\\b' src tests apps scripts` exits non-zero
  </acceptance_criteria>
  <done>The renamed tree behaves as a PEP 420 namespace package and stale top-level import paths are eliminated</done>
</task>

</tasks>

<verification>
- `uv sync`
- `uv run python scripts/rename_to_zeroth_core.py --help`
- `bash -n scripts/rewrite_zeroth_refs.sh`
- namespace smoke-import commands exit 0
- stale-import ripgrep exits non-zero
</verification>

<success_criteria>
- All Python code lives under `src/zeroth/core/`
- `zeroth` is an implicit namespace package, not a regular package
- Python and non-Python path references consistently use `zeroth.core.*`
- Packaging metadata matches the renamed install target
</success_criteria>
