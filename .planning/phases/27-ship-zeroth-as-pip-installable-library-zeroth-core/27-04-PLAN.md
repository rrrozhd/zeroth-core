---
phase: 27-ship-zeroth-as-pip-installable-library-zeroth-core
plan: 04
type: execute
wave: 4
depends_on: ["27-03"]
files_modified:
  - pyproject.toml
  - .github/workflows/ci.yml
  - .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/pytest-after-rename.log
  - .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/interrogate-baseline.txt
  - .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/interrogate-after.txt
  - src/zeroth/core
autonomous: true
requirements: [RENAME-04, RENAME-05]

must_haves:
  truths:
    - "The repo has checked-in `interrogate` configuration with `fail-under = 90` and Google-style docstring expectations"
    - "A CI workflow exists and runs Ruff, pytest, and interrogate against the renamed package"
    - "Baseline and post-rename interrogate output are captured as artifacts"
    - "Baseline and post-rename pytest output can be compared directly to prove no new failures or skips were introduced"
    - "The public `zeroth.core.*` surface reaches >=90% measured interrogate coverage"
  artifacts:
    - path: "pyproject.toml"
      provides: "Docstring tooling configuration and dev dependencies"
      contains: "fail-under = 90"
    - path: ".github/workflows/ci.yml"
      provides: "Repo-wide verification workflow for lint, tests, and docstrings"
      contains: "interrogate src/zeroth/core"
    - path: ".planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/interrogate-baseline.txt"
      provides: "Measured docstring baseline before filling gaps"
      contains: "%"
    - path: ".planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/interrogate-after.txt"
      provides: "Measured post-docstring coverage"
      contains: "%"
    - path: ".planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/pytest-after-rename.log"
      provides: "Post-rename test evidence"
      contains: "collected"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "workflow executes the exact tools configured in pyproject"
      pattern: "interrogate"
---

<objective>
Add the docstring tooling and CI gates required by Phase 27, fill enough real docstring gaps to reach the target, and capture the post-rename verification evidence proving the rename did not regress the test suite.

Purpose: Phase 27 is not complete until the renamed package is verified and the docstring threshold is enforced.
Output: interrogate/Ruff config, first CI workflow, docstring improvements on the public surface, and post-rename evidence artifacts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/27-CONTEXT.md
@.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/27-RESEARCH.md
@.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/27-03-PLAN.md
@pyproject.toml
@.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/pytest-before-rename.log
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interrogate, Google-style Ruff settings, and the first CI workflow</name>
  <files>
    pyproject.toml,
    .github/workflows/ci.yml,
    .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/interrogate-baseline.txt
  </files>
  <read_first>
    - pyproject.toml
    - .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/27-CONTEXT.md
    - .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/27-RESEARCH.md
  </read_first>
  <action>
Update `pyproject.toml` with these exact changes:
- add `interrogate` to `[dependency-groups].dev` if missing
- ensure `[tool.ruff.lint].select` includes `D`
- add `[tool.ruff.lint.pydocstyle]` with `convention = "google"`
- add `[tool.interrogate]` with:
  - `fail-under = 90`
  - `ignore-init-method = true`
  - `ignore-init-module = true`
  - `ignore-nested-functions = true`
  - `exclude = ["src/zeroth/core/migrations", "tests", "scripts", "apps"]`

Create `.github/workflows/ci.yml` as the repo's first CI workflow. It must run on `push` and `pull_request` and execute these exact commands:
- `uv sync --all-groups`
- `uv run ruff check src tests`
- `uv run pytest -v --no-header -ra`
- `uv run interrogate src/zeroth/core`

Before filling docstrings, run `uv run interrogate -v src/zeroth/core --fail-under 0 2>&1 | tee .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/interrogate-baseline.txt`.
  </action>
  <verify>
    <automated>uv run interrogate -v src/zeroth/core --fail-under 0 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `fail-under = 90`
    - `pyproject.toml` contains `convention = "google"`
    - `.github/workflows/ci.yml` exists
    - `.github/workflows/ci.yml` contains `uv sync --all-groups`
    - `.github/workflows/ci.yml` contains `uv run interrogate src/zeroth/core`
    - `.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/interrogate-baseline.txt` exists after execution
  </acceptance_criteria>
  <done>Docstring tooling and CI enforcement exist, and the baseline docstring-coverage measurement is captured</done>
</task>

<task type="auto">
  <name>Task 2: Raise public-surface docstring coverage to the target with prioritized real docstrings</name>
  <files>
    src/zeroth/core,
    .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/interrogate-after.txt
  </files>
  <read_first>
    - .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/interrogate-baseline.txt
    - src/zeroth/core/service
    - src/zeroth/core/deployments
    - src/zeroth/core/orchestrator
    - src/zeroth/core/graph
    - src/zeroth/core/execution_units
    - src/zeroth/core/agent_runtime
  </read_first>
  <action>
Add human-written Google-style docstrings until coverage reaches at least 90%, using this exact priority order:
1. public package `__init__.py` exports under `src/zeroth/core/**/__init__.py`
2. `src/zeroth/core/service/**`
3. `src/zeroth/core/deployments/**`
4. `src/zeroth/core/orchestrator/**`
5. `src/zeroth/core/graph/**`
6. `src/zeroth/core/execution_units/**`
7. `src/zeroth/core/agent_runtime/**`

Do not add placeholder docstrings such as `TODO`, `TBD`, or one-line non-descriptive fillers.

After the gap-filling pass, run:
- `uv run interrogate -v src/zeroth/core 2>&1 | tee .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/interrogate-after.txt`
- `uv run ruff check src tests`
  </action>
  <verify>
    <automated>uv run interrogate -v src/zeroth/core 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/interrogate-after.txt` exists after execution
    - `interrogate-after.txt` contains `RESULT: PASSED`
    - `uv run ruff check src tests` exits 0
  </acceptance_criteria>
  <done>Public-surface docstring coverage meets the threshold with enforceable tooling and non-placeholder content</done>
</task>

<task type="auto">
  <name>Task 3: Capture post-rename verification and compare it to the baseline</name>
  <files>
    .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/pytest-after-rename.log,
    .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/interrogate-after.txt
  </files>
  <read_first>
    - .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/pytest-before-rename.log
    - pyproject.toml
    - .github/workflows/ci.yml
  </read_first>
  <action>
Run:
- `uv run pytest -v --no-header -ra 2>&1 | tee .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/pytest-after-rename.log`
- `uv run python -c "from zeroth.core.orchestrator import RuntimeOrchestrator; print(RuntimeOrchestrator.__name__)"`

Then compare the baseline and post-rename pytest logs for:
- collected test count
- failures
- errors
- skipped tests

If the post-rename log introduces any new failure, error, or skip relative to `pytest-before-rename.log`, stop and fix the regression before marking the plan complete.
  </action>
  <verify>
    <automated>test -f .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/pytest-after-rename.log</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/pytest-after-rename.log` exists after execution
    - `uv run python -c "from zeroth.core.orchestrator import RuntimeOrchestrator; print(RuntimeOrchestrator.__name__)"` exits 0
    - post-rename pytest log shows no new failures relative to baseline
    - post-rename pytest log shows no new skips relative to baseline
  </acceptance_criteria>
  <done>Phase 27 has post-rename evidence proving the renamed package still passes the suite and import smoke tests</done>
</task>

</tasks>

<verification>
- `uv run interrogate -v src/zeroth/core`
- `uv run ruff check src tests`
- `uv run pytest -v --no-header -ra`
- `test -f .github/workflows/ci.yml`
</verification>

<success_criteria>
- Docstring tooling is configured and enforced
- CI exists and runs the required checks
- Public `zeroth.core.*` coverage reaches >=90%
- Post-rename verification proves the rename introduced no new failures or skips
</success_criteria>
