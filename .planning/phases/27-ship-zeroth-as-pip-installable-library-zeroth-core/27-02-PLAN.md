---
phase: 27-ship-zeroth-as-pip-installable-library-zeroth-core
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - scripts/publish_monolith_archive.sh
  - .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-github-publish.txt
  - .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-recovery-test.txt
autonomous: true
requirements: [ARCHIVE-01, ARCHIVE-02, ARCHIVE-03]

must_haves:
  truths:
    - "The local bare mirror is pushed to `rrrozhd/zeroth-archive` with `git push --mirror`"
    - "The archive repo README begins with a visible archived notice pointing to `rrrozhd/zeroth-core` and `rrrozhd/zeroth-studio`"
    - "The GitHub repo description matches the archived notice intent and the repo is archived in the GitHub UI"
    - "Remote recoverability evidence exists after the mirror push and README-banner commit"
  artifacts:
    - path: "scripts/publish_monolith_archive.sh"
      provides: "Repeatable GitHub archive publication workflow"
      contains: "gh repo create rrrozhd/zeroth-archive"
    - path: ".planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-github-publish.txt"
      provides: "Output from repo creation, mirror push, description update, README notice commit, and repo archival"
      contains: "rrrozhd/zeroth-archive"
    - path: ".planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-recovery-test.txt"
      provides: "Post-push remote recovery evidence"
      contains: "archive/detached-wt-"
  key_links:
    - from: "scripts/publish_monolith_archive.sh"
      to: ".planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-github-publish.txt"
      via: "script records all GitHub/archive operations"
      pattern: "archive-github-publish.txt"
---

<objective>
Publish the already-created local bare mirror to GitHub as `rrrozhd/zeroth-archive`, add the visible archive notice, archive the GitHub repo, and record evidence that the remote copy is recoverable.

Purpose: Phase 27 requires three archive layers, not just the local ones.
Output: a checked-in publication script plus artifact logs proving the GitHub archive exists and carries the correct notice.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/27-CONTEXT.md
@.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/27-RESEARCH.md
@.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/27-01-PLAN.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the GitHub archive publication script with README-banner and repo-archive steps</name>
  <files>
    scripts/publish_monolith_archive.sh,
    .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-github-publish.txt
  </files>
  <read_first>
    - scripts/archive_monolith.sh
    - scripts/verify_monolith_archive.sh
    - README.md
    - .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/27-CONTEXT.md
  </read_first>
  <action>
Create `scripts/publish_monolith_archive.sh` as a checked-in Bash script with `set -euo pipefail` that performs these exact operations:
- verify `$HOME/archives/zeroth-monolith/zeroth-monolith.git` exists
- `gh repo view rrrozhd/zeroth-archive >/dev/null 2>&1 || gh repo create rrrozhd/zeroth-archive --public --description "Archived monolithic Zeroth repo — see rrrozhd/zeroth-core and rrrozhd/zeroth-studio"`
- add `origin` in the bare mirror if missing
- run `git -C "$HOME/archives/zeroth-monolith/zeroth-monolith.git" push --mirror origin`
- clone the archive repo into `/tmp/zeroth-archive-readme`
- prepend this exact banner to the top of `README.md` in the archive repo:
  `⚠️ **This repository is archived.** Active development continues in \`rrrozhd/zeroth-core\` (Python library) and \`rrrozhd/zeroth-studio\` (Vue frontend). This repo exists only to preserve the pre-split monolith history.`
- commit the README change in the archive repo and push it
- run `gh repo edit rrrozhd/zeroth-archive --description "Archived monolithic Zeroth repo — see rrrozhd/zeroth-core and rrrozhd/zeroth-studio"`
- run `gh repo archive rrrozhd/zeroth-archive --yes`

Write stdout/stderr from every GitHub operation to `.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-github-publish.txt`.
  </action>
  <verify>
    <automated>bash -n scripts/publish_monolith_archive.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/publish_monolith_archive.sh` exists
    - `scripts/publish_monolith_archive.sh` contains `gh repo create rrrozhd/zeroth-archive`
    - `scripts/publish_monolith_archive.sh` contains `git -C \"$HOME/archives/zeroth-monolith/zeroth-monolith.git\" push --mirror origin`
    - `scripts/publish_monolith_archive.sh` contains `gh repo archive rrrozhd/zeroth-archive --yes`
    - `scripts/publish_monolith_archive.sh` contains `rrrozhd/zeroth-core`
    - `bash -n scripts/publish_monolith_archive.sh` exits 0
  </acceptance_criteria>
  <done>GitHub archive publication is automated and includes the required README banner plus repo-description/archive flag updates</done>
</task>

<task type="auto">
  <name>Task 2: Publish the GitHub archive and capture remote recoverability evidence</name>
  <files>
    .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-github-publish.txt,
    .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-recovery-test.txt
  </files>
  <read_first>
    - scripts/publish_monolith_archive.sh
    - scripts/verify_monolith_archive.sh
    - .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-preflight.txt
  </read_first>
  <action>
Execute `bash scripts/publish_monolith_archive.sh`.

After the mirror push and README-banner commit succeed, write `.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-recovery-test.txt` by:
- cloning `git@github.com:rrrozhd/zeroth-archive.git` into `/tmp/zeroth-archive-recovery`
- checking out one normal branch
- checking out one `archive/stash-*` branch
- checking out one `archive/detached-wt-*` branch
- recording `git branch --show-current` and `git log --oneline -5` output for each checkout

If `gh` authentication or repo-creation permission blocks publication, stop after writing the failure output into `archive-github-publish.txt`; do not continue to Plan 27-03 until the GitHub archive step is complete.
  </action>
  <verify>
    <automated>test -f .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-github-publish.txt</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-github-publish.txt` exists after execution
    - `.planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-recovery-test.txt` exists after successful publication
    - `archive-github-publish.txt` contains `rrrozhd/zeroth-archive`
    - `archive-recovery-test.txt` contains `archive/stash-`
    - `archive-recovery-test.txt` contains `archive/detached-wt-`
  </acceptance_criteria>
  <done>GitHub archive publication and post-push recovery checks are evidenced locally</done>
</task>

</tasks>

<verification>
- `bash -n scripts/publish_monolith_archive.sh`
- `test -f .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-github-publish.txt`
- `test -f .planning/phases/27-ship-zeroth-as-pip-installable-library-zeroth-core/artifacts/archive-recovery-test.txt`
</verification>

<success_criteria>
- The GitHub archive layer exists in addition to the tarball and local mirror
- The archive repo carries the required visible notice and archived repo description
- Remote recoverability of normal, stash, and detached-worktree refs is proven with an artifact
</success_criteria>
