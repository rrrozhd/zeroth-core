---
phase: 22-canvas-foundation-dev-infrastructure
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/zeroth/service/studio_api.py
autonomous: true
gap_closure: true
requirements:
  - CANV-06
  - API-01

must_haves:
  truths:
    - "All 6 CRUD endpoint functions in studio_api.py are async def"
    - "All repository calls (repo.save, repo.get, repo.list) are awaited"
    - "All 10 tests in test_studio_api.py pass"
  artifacts:
    - path: "src/zeroth/service/studio_api.py"
      provides: "Async Studio API endpoints"
      contains: "async def"
  key_links:
    - from: "src/zeroth/service/studio_api.py"
      to: "graph/repository.py"
      via: "await repo.save/get/list"
      pattern: "await repo\\.(save|get|list)"
---

<objective>
Fix async/await mismatch in Studio API endpoints that breaks all CRUD operations.

Purpose: All 6 endpoint functions in studio_api.py use sync `def` but call async repository methods (repo.save, repo.get, repo.list) without await, returning coroutine objects instead of data. This blocks CANV-06 (save/load workflows) and API-01 (CRUD endpoints).

Output: Working Studio API with all 10 backend tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-canvas-foundation-dev-infrastructure/22-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix async/await mismatch in Studio API endpoints</name>
  <files>src/zeroth/service/studio_api.py</files>

  <read_first>
    - src/zeroth/service/studio_api.py (current broken state -- all 6 endpoints are sync def)
    - tests/test_studio_api.py (to understand expected behavior and test assertions)
  </read_first>

  <action>
    In src/zeroth/service/studio_api.py, make these exact changes to all 6 endpoint functions:

    1. Line 198: Change `def list_workflows(` to `async def list_workflows(`
       Line 201: Change `graphs = repo.list()` to `graphs = await repo.list()`

    2. Line 211: Change `def create_workflow(` to `async def create_workflow(`
       Line 230: Change `saved = repo.save(graph)` to `saved = await repo.save(graph)`

    3. Line 235: Change `def get_workflow(` to `async def get_workflow(`
       Line 238: Change `graph = repo.get(workflow_id)` to `graph = await repo.get(workflow_id)`

    4. Line 245: Change `def update_workflow(` to `async def update_workflow(`
       Line 248: Change `graph = repo.get(workflow_id)` to `graph = await repo.get(workflow_id)`
       Line 277: Change `saved = repo.save(updated_graph)` to `saved = await repo.save(updated_graph)`

    5. Line 284: Change `def delete_workflow(` to `async def delete_workflow(`
       Line 286: Change `graph = repo.get(workflow_id)` to `graph = await repo.get(workflow_id)`
       Line 292: Change `repo.save(archived)` to `await repo.save(archived)`

    6. The node-types endpoint (line 297, `def list_node_types`) does NOT call any async methods -- leave it as sync `def`. It only returns the static _NODE_TYPES list.

    Do NOT change _get_graph_repository, _graph_to_detail, _graph_to_summary, or any other helper function. Only change the 5 endpoint functions that call repository methods.
  </action>

  <verify>
    <automated>cd /Users/dondoe/coding/zeroth && uv run pytest tests/test_studio_api.py -v</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "async def list_workflows" src/zeroth/service/studio_api.py returns 1
    - grep -c "async def create_workflow" src/zeroth/service/studio_api.py returns 1
    - grep -c "async def get_workflow" src/zeroth/service/studio_api.py returns 1
    - grep -c "async def update_workflow" src/zeroth/service/studio_api.py returns 1
    - grep -c "async def delete_workflow" src/zeroth/service/studio_api.py returns 1
    - grep -c "await repo\." src/zeroth/service/studio_api.py returns 8 (save x3, get x3, list x1, save x1 in delete)
    - grep -c "def list_node_types" src/zeroth/service/studio_api.py returns 1 (stays sync, no async)
    - uv run pytest tests/test_studio_api.py -v shows 10 passed, 0 failed
  </acceptance_criteria>

  <done>All 10 Studio API tests pass. The 5 CRUD endpoints are async def with all repo calls awaited. The node-types endpoint remains sync. CANV-06 and API-01 are unblocked.</done>
</task>

</tasks>

<verification>
Run the full Studio API test suite:
```bash
uv run pytest tests/test_studio_api.py -v
```
Expected: 10 passed, 0 failed.

Run linter to confirm no regressions:
```bash
uv run ruff check src/zeroth/service/studio_api.py
```
</verification>

<success_criteria>
- All 10 tests in test_studio_api.py pass (currently 8 fail)
- Studio CRUD operations return actual Graph data, not coroutine objects
- No lint errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/22-canvas-foundation-dev-infrastructure/22-06-SUMMARY.md`
</output>
